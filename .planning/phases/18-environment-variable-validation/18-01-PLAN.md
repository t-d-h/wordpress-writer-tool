---
phase: 18-environment-variable-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/config.py, .planning/REQUIREMENTS.md]
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04, CONF-05]
user_setup: []
must_haves:
  truths:
    - "System logs an error when INIT_USER is missing or empty"
    - "System logs an error when INIT_PASSWORD is missing or empty"
    - "System does not log when environment variables are successfully loaded"
    - "CONF-05 requirement reflects fail-fast approach (no defaults)"
  artifacts:
    - path: "backend/app/config.py"
      provides: "Environment variable validation with error logging"
      min_lines: 35
      contains: "import logging", "logger.error"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement reflecting fail-fast approach"
      contains: "CONF-05"
  key_links:
    - from: "backend/app/config.py"
      to: "validate() method"
      via: "logger.error() before ValueError"
      pattern: "logger\\.error.*INIT_USER|INIT_PASSWORD"
    - from: ".planning/REQUIREMENTS.md"
      to: "CONF-05 requirement"
      via: "text update"
      pattern: "CONF-05.*fail-fast|no defaults"
---

<objective>
Add error logging to environment variable validation and update requirements to reflect fail-fast approach.

Purpose: Provide clear error logging when required environment variables are missing, while maintaining the fail-fast behavior established in Phase 17. Update CONF-05 requirement to accurately reflect the no-defaults decision.

Output: Updated config.py with error logging and corrected REQUIREMENTS.md
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-environment-variable-validation/18-CONTEXT.md
@.planning/phases/17-configuration-layer/17-01-SUMMARY.md

# Existing Configuration
From backend/app/config.py:
```python
class Settings:
    INIT_USER: str = os.getenv("INIT_USER")
    INIT_PASSWORD: str = os.getenv("INIT_PASSWORD")

    def validate(self) -> bool:
        """Validate that required configuration fields are present and non-empty."""
        if not self.INIT_USER:
            raise ValueError(
                "INIT_USER environment variable is required and cannot be empty"
            )
        if not self.INIT_PASSWORD:
            raise ValueError(
                "INIT_PASSWORD environment variable is required and cannot be empty"
            )
        return True
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error logging to validate() method</name>
  <files>backend/app/config.py</files>
  <action>Add logging import at top of file: `import logging`. Initialize logger: `logger = logging.getLogger(__name__)`. In validate() method, add `logger.error()` call before each ValueError raise. Log message should be: "INIT_USER environment variable is missing or empty" and "INIT_PASSWORD environment variable is missing or empty". Per D-04, D-05, D-06: log only on error, no logging on successful load, minimal logging approach.</action>
  <verify>grep -q "import logging" backend/app/config.py && grep -q "logger.error" backend/app/config.py && grep -q "INIT_USER environment variable is missing or empty" backend/app/config.py</verify>
  <done>validate() method logs error before raising ValueError for missing INIT_USER or INIT_PASSWORD</done>
</task>

<task type="auto">
  <name>Task 2: Update CONF-05 requirement to reflect fail-fast approach</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>Update CONF-05 requirement from "System provides sensible default values if environment variables are missing" to "System fails fast if INIT_USER or INIT_PASSWORD are missing or empty (no default values)". This aligns with D-01, D-02, D-03 decisions that no defaults should be provided.</action>
  <verify>grep -A 1 "CONF-05" .planning/REQUIREMENTS.md | grep -q "fails fast.*no default"</verify>
  <done>CONF-05 requirement accurately reflects fail-fast approach with no defaults</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment → Application | Untrusted environment variables cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | config.py validate() | accept | Low-value target, network isolation provides protection. Error messages are generic, no sensitive info leaked. |
| T-18-02 | Information Disclosure | config.py logger.error() | accept | Error messages do not contain sensitive information, only variable names. |
| T-18-03 | Denial of Service | config.py validate() | accept | Fail-fast behavior is intentional, not a vulnerability. System cannot start without required credentials. |
</threat_model>

<verification>
- config.py imports logging module
- config.py initializes logger
- validate() method calls logger.error() before raising ValueError for INIT_USER
- validate() method calls logger.error() before raising ValueError for INIT_PASSWORD
- No logging occurs when environment variables are successfully loaded
- CONF-05 requirement updated to reflect fail-fast approach
- System still fails fast on startup if INIT_USER or INIT_PASSWORD are missing
</verification>

<success_criteria>
- Error logging added to validate() method for missing INIT_USER and INIT_PASSWORD
- Logging only occurs on error, not on successful load
- CONF-05 requirement updated to reflect fail-fast approach (no defaults)
- System maintains fail-fast behavior from Phase 17
</success_criteria>

<output>
After completion, create `.planning/phases/18-environment-variable-validation/18-01-SUMMARY.md`
</output>

---
phase: 04-backend-api-enhancement
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - backend/app/routers/wp_sites.py
autonomous: true
requirements:
  - BACKEND-04
  - BACKEND-05
  - BACKEND-06
user_setup: []
must_haves:
  truths:
    - get_site_posts uses hybrid pagination (cache first, WordPress API fallback)
    - Cache is checked before WordPress API call
    - Stale cache triggers WordPress API fallback
    - Search always bypasses cache and hits WordPress API
    - Cache refresh endpoint is available
    - Cache refresh provides progress feedback
  artifacts:
    - path: backend/app/routers/wp_sites.py
      provides: Hybrid pagination with cache integration
      exports: ["get_site_posts", "refresh_site_posts_cache"]
      min_lines: 150
  key_links:
    - from: backend/app/routers/wp_sites.py
      to: backend/app/services/wp_cache_service.py
      via: "WPCacheService for cache operations"
      pattern: "WPCacheService|wp_cache_service"
    - from: backend/app/routers/wp_sites.py
      to: backend/app/services/wp_service.py
      via: "get_wp_posts() for WordPress API fallback"
      pattern: "get_wp_posts"
</key_links>
---

<objective>
Integrate cache service with get_site_posts endpoint to implement hybrid pagination and add cache refresh endpoint.

Purpose: Improve performance by using cached data when available while ensuring freshness through staleness detection and WordPress API fallback. Provide manual cache refresh capability.

Output: Enhanced get_site_posts endpoint with hybrid pagination and new refresh_site_posts_cache endpoint.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-backend-api-enhancement/04-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@backend/app/routers/wp_sites.py
@backend/app/services/wp_cache_service.py
@backend/app/services/wp_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate cache service into get_site_posts endpoint</name>
  <files>backend/app/routers/wp_sites.py</files>
  <action>Update get_site_posts endpoint in backend/app/routers/wp_sites.py to use cache service:

1. Import WPCacheService at top of file:
   from app.services.wp_cache_service import WPCacheService

2. Initialize cache service instance in endpoint function:
   cache_service = WPCacheService()

3. Generate cache key using cache service:
   cache_key = cache_service.get_cache_key(str(project["_id"]), per_page, page, status, orderby, order)

4. Implement hybrid pagination logic:
   - If search parameter is provided: bypass cache, call WordPress API directly (per D-09 decision)
   - If no search parameter:
     a. Check cache for existing data
     b. If cache exists and is not stale: return cached data
     c. If cache is stale or missing: call WordPress API, update cache, return fresh data

5. Update cache after WordPress API call:
   cache_service.cache_posts(cache_key, result["posts"], result["total"])

Per D-14 decision: Pagination uses hybrid approach - paginate cache first, fall back to WordPress API if cache is stale or missing.
Per D-15 decision: Hybrid pagination falls back to WordPress API if cache is stale or missing.
Per D-16 decision: Cache staleness detected by comparing with WordPress API.

Add logging for cache hits, misses, and staleness detection.
</action>
  <verify>
    <automated>grep -E "WPCacheService|cache_service" backend/app/routers/wp_sites.py | wc -l</automated>
  </verify>
  <done>Cache service integrated into get_site_posts endpoint with hybrid pagination</done>
</task>

<task type="auto">
  <name>Task 2: Add cache refresh endpoint</name>
  <files>backend/app/routers/wp_sites.py</files>
  <action>Add new endpoint for manual cache refresh in backend/app/routers/wp_sites.py:

@router.post("/{site_id}/posts/refresh")
async def refresh_site_posts_cache(
    site_id: str,
    per_page: int = 100,
    page: int = 1,
    status: str = None,
    orderby: str = "date",
    order: str = "desc"
):
    """Manually refresh cached WordPress posts for a site."""
    from app.services.wp_cache_service import WPCacheService

    # Verify site exists
    doc = await wp_sites_col.find_one({"_id": ObjectId(site_id)})
    if not doc:
        raise HTTPException(status_code=404, detail="Site not found")

    # Get project
    project = await projects_col.find_one({"wp_site_id": site_id})
    if not project:
        raise HTTPException(status_code=404, detail="No project found for this site")

    # Refresh cache
    cache_service = WPCacheService()
    result = await cache_service.refresh_cache(
        str(project["_id"]), per_page, page, status, orderby, order
    )

    return result

Per D-05 decision: Manual refresh triggered via admin UI button.
Per D-06 decision: Cache refresh provides feedback with loading spinner during refresh, then success message.

Add endpoint after get_site_posts endpoint (after line 119).
</action>
  <verify>
    <automated>grep -A 5 "refresh_site_posts_cache" backend/app/routers/wp_sites.py | grep "async def"</automated>
  </verify>
  <done>Cache refresh endpoint added with progress feedback</done>
</task>

<task type="auto">
  <name>Task 3: Implement cache bypass for search queries</name>
  <files>backend/app/routers/wp_sites.py</files>
  <action>Update get_site_posts endpoint to bypass cache when search parameter is provided:

In the hybrid pagination logic, add check at the beginning:
if search:
    # Search always hits WordPress API (not cached data)
    result = await get_wp_posts(str(project["_id"]), per_page, page, status, search, orderby, order)
    return result

Per D-09 decision: Search always hits WordPress API with search parameter (not cached data).

This ensures search queries always return fresh results from WordPress API, bypassing the cache entirely.

Add logging for search bypass.
</action>
  <verify>
    <automated>grep -A 3 "if search:" backend/app/routers/wp_sites.py | grep "get_wp_posts"</automated>
  </verify>
  <done>Cache bypass implemented for search queries</done>
</task>

<task type="auto">
  <name>Task 4: Add error handling for cache operations</name>
  <files>backend/app/routers/wp_sites.py</files>
  <action>Add comprehensive error handling for cache operations in get_site_posts endpoint:

1. Wrap cache operations in try/except blocks
2. If cache retrieval fails, log error and fall back to WordPress API
3. If cache staleness check fails, log error and treat as stale
4. If cache update fails, log error but continue (non-critical)
5. If WordPress API fails, raise HTTPException with appropriate error message

Example error handling pattern:
try:
    cached = cache_service.get_cached_posts(cache_key)
    if cached and not cache_service.is_cache_stale(cache_key):
        return cached
except Exception as e:
    print(f"[CACHE_ERROR] Failed to retrieve cache: {str(e)}")
    # Fall back to WordPress API

Follow existing error handling pattern in wp_sites.py (lines 44-47, 110-115).

Add logging for all cache-related errors.
</action>
  <verify>
    <automated>grep -E "try:|except.*Exception" backend/app/routers/wp_sites.py | wc -l</automated>
  </verify>
  <done>Error handling added for cache operations</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cache → Backend | Cached data (potentially stale) crosses here |
| Backend → WordPress API | Fallback API calls for stale/missing cache |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | D | get_site_posts() | mitigate | Validate cache key format before cache operations |
| T-04-12 | D | refresh_site_posts_cache() | mitigate | Implement rate limiting on refresh endpoint |
| T-04-13 | I | get_site_posts() | mitigate | Sanitize cached data before returning |
| T-04-14 | R | get_site_posts() | accept | Cache staleness detection is informational |
| T-04-15 | T | cache operations | accept | Cache errors fall back to WordPress API |
</threat_model>

<verification>
- get_site_posts uses hybrid pagination with cache integration
- Cache is checked before WordPress API call
- Stale cache triggers WordPress API fallback
- Search bypasses cache and hits WordPress API
- Cache refresh endpoint is available
- Error handling covers all cache operations
</verification>

<success_criteria>
- Hybrid pagination implemented with cache first, WordPress API fallback
- Cache refresh endpoint provides progress feedback
- Search bypasses cache and hits WordPress API
- Error handling covers all cache operations
- Logging provides visibility into cache behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-backend-api-enhancement/04-03-SUMMARY.md`
</output>

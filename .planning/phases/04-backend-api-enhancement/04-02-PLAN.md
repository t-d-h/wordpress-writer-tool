---
phase: 04-backend-api-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/wp_sites.py
  - backend/app/services/wp_service.py
autonomous: true
requirements:
  - BACKEND-01
  - BACKEND-02
  - BACKEND-03
user_setup: []
must_haves:
  truths:
    - Backend exposes search, orderby, and order parameters in get_site_posts endpoint
    - Backend supports 100 posts per page pagination
    - Backend returns WordPress REST API data with proper transformation
    - Search always hits WordPress API (not cached data)
    - Search queries post titles only
    - Search is case-insensitive
    - Search supports partial matching
    - Search results limited to 100 posts
  artifacts:
    - path: backend/app/routers/wp_sites.py
      provides: Updated get_site_posts endpoint with search/sort parameters
      exports: ["get_site_posts"]
      min_lines: 120
    - path: backend/app/services/wp_service.py
      provides: Enhanced get_wp_posts with search/sort support
      exports: ["get_wp_posts"]
      min_lines: 300
  key_links:
    - from: backend/app/routers/wp_sites.py
      to: backend/app/services/wp_service.py
      via: "get_wp_posts() call with search/sort parameters"
      pattern: "get_wp_posts"
    - from: backend/app/routers/wp_sites.py
      to: WordPress REST API
      via: "HTTP request with search/orderby/order parameters"
      pattern: "search|orderby|order"
</key_links>
---

<objective>
Update get_site_posts endpoint to expose search, orderby, and order parameters with 100 posts per page pagination and proper data transformation.

Purpose: Enable frontend to search, sort, and paginate WordPress posts while maintaining existing functionality and following WordPress REST API conventions.

Output: Enhanced get_site_posts endpoint with search, orderby, order parameters and 100 posts per page pagination.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-backend-api-enhancement/04-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@backend/app/routers/wp_sites.py
@backend/app/services/wp_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update get_site_posts endpoint to accept search, orderby, and order parameters</name>
  <files>backend/app/routers/wp_sites.py</files>
  <action>Update get_site_posts endpoint in backend/app/routers/wp_sites.py (line 102-119):

1. Add search parameter: search: str = None
2. Add orderby parameter: orderby: str = "date"
3. Add order parameter: order: str = "desc"
4. Update per_page default to 100 (already set)
5. Pass all parameters to get_wp_posts() call

Updated endpoint signature:
@router.get("/{site_id}/posts")
async def get_site_posts(
    site_id: str,
    per_page: int = 100,
    page: int = 1,
    status: str = None,
    search: str = None,
    orderby: str = "date",
    order: str = "desc"
):

Update get_wp_posts() call to pass all new parameters:
result = await get_wp_posts(str(project["_id"]), per_page, page, status, search, orderby, order)

Per D-20 decision: Backend exposes search, orderby, and order parameters in get_site_posts endpoint.
Per D-21 decision: Backend supports 100 posts per page pagination.
</action>
  <verify>
    <automated>grep -A 10 "async def get_site_posts" backend/app/routers/wp_sites.py | grep -E "search|orderby|order"</automated>
  </verify>
  <done>get_site_posts endpoint updated with search, orderby, and order parameters</done>
</task>

<task type="auto">
  <name>Task 2: Verify get_wp_posts supports search, orderby, and order parameters</name>
  <files>backend/app/services/wp_service.py</files>
  <action>Verify get_wp_posts() function in backend/app/services/wp_service.py (line 258-295) already supports:

1. search parameter (line 263): Search by post title/content
2. orderby parameter (line 264): Order by field (date, title, modified, etc.)
3. order parameter (line 265): ASC or DESC

The function already passes these parameters to WordPress REST API via params dict (lines 286-293).

If parameters are missing, add them following the existing pattern:
- Add search: str = None to function signature
- Add orderby: str = "date" to function signature
- Add order: str = "desc" to function signature
- Add params["search"] = search if search
- Add params["orderby"] = orderby if orderby
- Add params["order"] = order if order

Per D-09 decision: Search always hits WordPress API (not cached data).
Per D-10 decision: Search queries post titles only (WordPress REST API default behavior).
Per D-11 decision: Search is case-insensitive (WordPress REST API default behavior).
Per D-12 decision: Search supports partial matching (WordPress REST API default behavior).
Per D-13 decision: Search results limited to 100 posts (enforced by per_page parameter).
</action>
  <verify>
    <automated>grep -A 5 "async def get_wp_posts" backend/app/services/wp_service.py | grep -E "search|orderby|order"</automated>
  </verify>
  <done>get_wp_posts verified to support search, orderby, and order parameters</done>
</task>

<task type="auto">
  <name>Task 3: Ensure proper data transformation in get_wp_posts response</name>
  <files>backend/app/services/wp_service.py</files>
  <action>Verify get_wp_posts() function returns properly transformed data:

1. Check that function returns dict with 'posts' list and 'total' count (line 64)
2. Verify posts include _embedded data for categories and tags (line 285: "_embed": True)
3. Ensure response structure matches WordPress REST API format

The function already returns {"posts": posts, "total": total} which is the correct format.

Per D-19 decision: Backend returns WordPress REST API data with proper transformation.

If transformation is needed, add helper function to format WordPress posts:
- Extract categories from post._embedded['wp:term'][0]
- Extract tags from post._embedded['wp:term'][1]
- Format dates for display
- Generate edit URLs for WordPress admin

However, for this phase, the WordPress REST API response format is sufficient as-is. Data transformation will be handled in Phase 5 (Data Transformation).
</action>
  <verify>
    <automated>grep -A 2 "return {" backend/app/services/wp_service.py | grep "posts.*total"</automated>
  </verify>
  <done>Data transformation verified in get_wp_posts response</done>
</task>

<task type="auto">
  <name>Task 4: Add parameter validation and error handling</name>
  <files>backend/app/routers/wp_sites.py</files>
  <action>Add parameter validation and error handling to get_site_posts endpoint:

1. Validate orderby parameter is one of allowed values: date, title, modified, relevance
   If invalid, raise HTTPException(status_code=400, detail="Invalid orderby parameter")

2. Validate order parameter is either "asc" or "desc" (case-insensitive)
   If invalid, raise HTTPException(status_code=400, detail="Invalid order parameter")

3. Validate per_page is between 1 and 100
   If invalid, raise HTTPException(status_code=400, detail="per_page must be between 1 and 100")

4. Validate page is >= 1
   If invalid, raise HTTPException(status_code=400, detail="page must be >= 1")

Add validation after site_id lookup and before get_wp_posts() call.

Follow existing error handling pattern in wp_sites.py (lines 44-47, 110-115).
</action>
  <verify>
    <automated>grep -E "HTTPException.*400" backend/app/routers/wp_sites.py | wc -l</automated>
  </verify>
  <done>Parameter validation and error handling added to get_site_posts endpoint</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend | User-provided search/sort parameters cross here |
| Backend → WordPress API | Validated parameters sent to external API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | I | get_site_posts() | mitigate | Validate orderby parameter against allowlist |
| T-04-07 | I | get_site_posts() | mitigate | Validate order parameter is "asc" or "desc" |
| T-04-08 | I | get_site_posts() | mitigate | Validate per_page is between 1 and 100 |
| T-04-09 | I | get_site_posts() | mitigate | Validate page is >= 1 |
| T-04-10 | D | get_wp_posts() | accept | WordPress API handles search injection protection |
</threat_model>

<verification>
- get_site_posts endpoint accepts search, orderby, and order parameters
- get_wp_posts function supports all parameters
- Parameter validation prevents invalid input
- Error handling follows existing patterns
- Response format matches WordPress REST API
</verification>

<success_criteria>
- Backend exposes search, orderby, and order parameters
- Backend supports 100 posts per page pagination
- Backend returns WordPress REST API data with proper transformation
- Parameter validation prevents invalid input
- Error handling follows existing patterns
</success_criteria>

<output>
After completion, create `.planning/phases/04-backend-api-enhancement/04-02-SUMMARY.md`
</output>

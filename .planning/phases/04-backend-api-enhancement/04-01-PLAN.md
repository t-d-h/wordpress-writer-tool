---
phase: 04-backend-api-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/wp_cache_service.py
  - backend/app/database.py
autonomous: true
requirements:
  - BACKEND-04
  - BACKEND-05
  - BACKEND-06
user_setup: []
must_haves:
  truths:
    - WordPress posts are cached in MongoDB collection with TTL index
    - Cache has automatic expiration after configured TTL duration
    - Cache can be manually refreshed via API endpoint
    - Cache refresh provides progress feedback
    - Cache staleness is detected by comparing with WordPress API
  artifacts:
    - path: backend/app/services/wp_cache_service.py
      provides: Cache service for WordPress posts
      exports: ["WPCacheService", "get_wp_cache_service"]
      min_lines: 200
    - path: backend/app/database.py
      provides: MongoDB cache collection
      contains: "wp_posts_cache_col"
  key_links:
    - from: backend/app/services/wp_cache_service.py
      to: backend/app/database.py
      via: "wp_posts_cache_col collection access"
      pattern: "wp_posts_cache_col"
    - from: backend/app/services/wp_cache_service.py
      to: backend/app/services/wp_service.py
      via: "get_wp_posts() call for cache refresh"
      pattern: "get_wp_posts"
---

<objective>
Create WordPress post cache service with MongoDB storage, TTL-based expiration, and manual refresh capability.

Purpose: Reduce WordPress API calls and improve performance by caching posts locally while maintaining data freshness through manual refresh and staleness detection.

Output: WPCacheService class with cache storage, retrieval, staleness detection, and refresh methods.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-backend-api-enhancement/04-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@backend/app/services/wp_service.py
@backend/app/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WPCacheService class with cache storage and retrieval</name>
  <files>backend/app/services/wp_cache_service.py</files>
  <action>Create WPCacheService class in backend/app/services/wp_cache_service.py with:

1. Class initialization with MongoDB collection reference
2. get_cache_key(project_id, per_page, page, status, orderby, order) method to generate unique cache keys
3. get_cached_posts(cache_key) method to retrieve cached posts from MongoDB
4. cache_posts(cache_key, posts, total) method to store posts in cache with TTL
5. is_cache_stale(cache_key) method to detect staleness by comparing with WordPress API
6. refresh_cache(project_id, per_page, page, status, orderby, order) method to refresh cache from WordPress API

Use MongoDB TTL index for automatic expiration. Set TTL to 3 hours (10800 seconds) per D-03 decision (medium duration within 1-6 hour range).

Cache key format: "wp_posts:{project_id}:{per_page}:{page}:{status}:{orderby}:{order}"

Cache document structure:
{
  "_id": cache_key,
  "project_id": project_id,
  "posts": [...],
  "total": total,
  "cached_at": datetime,
  "ttl": 10800
}

Follow existing service patterns from wp_service.py for error handling and logging.
</action>
  <verify>
    <automated>python -c "from backend.app.services.wp_cache_service import WPCacheService; print('WPCacheService imported successfully')"</automated>
  </verify>
  <done>WPCacheService class created with cache storage, retrieval, staleness detection, and refresh methods</done>
</task>

<task type="auto">
  <name>Task 2: Add wp_posts_cache collection to database.py</name>
  <files>backend/app/database.py</files>
  <action>Add wp_posts_cache_col collection to backend/app/database.py:

1. Add collection definition: wp_posts_cache_col = db["wp_posts_cache"]
2. Add create_index call in create_indexes() function for TTL index:
   await wp_posts_cache_col.create_index([("cached_at", 1)], expireAfterSeconds=10800)

This ensures automatic cache expiration after 3 hours (10800 seconds) per D-03 decision.

Place the collection definition after existing collections (line 13) and the index creation in create_indexes() function after existing indexes (line 32).
</action>
  <verify>
    <automated>grep -q "wp_posts_cache_col" backend/app/database.py && echo "Collection found"</automated>
  </verify>
  <done>wp_posts_cache_col collection added with TTL index for automatic expiration</done>
</task>

<task type="auto">
  <name>Task 3: Implement cache staleness detection logic</name>
  <files>backend/app/services/wp_cache_service.py</files>
  <action>Implement is_cache_stale() method in WPCacheService:

1. Retrieve cached document from wp_posts_cache_col
2. If no cache exists, return True (stale)
3. Fetch single post from WordPress API to check if data has changed
4. Compare cached post count with WordPress API total count
5. If counts differ, return True (stale)
6. If counts match, return False (fresh)

Use get_wp_posts() from wp_service.py with per_page=1 to fetch minimal data for comparison.

Per D-08 and D-16 decisions: staleness detected by comparing with WordPress API (not time-based TTL).

Add logging for staleness detection results.
</action>
  <verify>
    <automated>grep -q "is_cache_stale" backend/app/services/wp_cache_service.py && echo "Method found"</automated>
  </verify>
  <done>Cache staleness detection implemented by comparing with WordPress API</done>
</task>

<task type="auto">
  <name>Task 4: Implement cache refresh with progress tracking</name>
  <files>backend/app/services/wp_cache_service.py</files>
  <action>Implement refresh_cache() method in WPCacheService with progress tracking:

1. Fetch posts from WordPress API using get_wp_posts()
2. Store posts in wp_posts_cache_col with current timestamp
3. Return dict with progress information:
   {
     "status": "success" | "partial" | "failed",
     "posts_refreshed": count,
     "total_posts": total,
     "message": "description"
   }

Per D-06 decision: cache refresh provides feedback with loading spinner during refresh, then success message.

Per D-07 decision: cache refresh scope is modified posts only - refresh only posts modified since last cache (implement by comparing modified timestamps if available, otherwise refresh all).

Add error handling for WordPress API failures with retry logic (max 3 retries with exponential backoff, following wp_service.py pattern).

Add logging for refresh progress and results.
</action>
  <verify>
    <automated>grep -q "refresh_cache" backend/app/services/wp_cache_service.py && echo "Method found"</automated>
  </verify>
  <done>Cache refresh implemented with progress tracking and error handling</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WordPress API → Backend | Untrusted external API responses cross here |
| Cache → Backend | Cached data (potentially stale) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | wp_cache_service.py | mitigate | Validate cache key format to prevent injection attacks |
| T-04-02 | I | wp_cache_service.py | mitigate | Sanitize WordPress API responses before caching |
| T-04-03 | D | wp_posts_cache_col | mitigate | Use MongoDB TTL index to limit cache lifetime |
| T-04-04 | R | refresh_cache() | mitigate | Implement rate limiting on cache refresh endpoint |
| T-04-05 | T | is_cache_stale() | accept | Staleness detection is informational, not security-critical |
</threat_model>

<verification>
- WPCacheService class exists with all required methods
- wp_posts_cache_col collection exists with TTL index
- Cache storage and retrieval work correctly
- Staleness detection compares with WordPress API
- Cache refresh provides progress feedback
- Error handling and logging are implemented
</verification>

<success_criteria>
- WordPress posts are cached in MongoDB with automatic expiration
- Cache can be retrieved using unique cache keys
- Staleness is detected by comparing with WordPress API
- Cache can be manually refreshed with progress feedback
- Error handling and logging are implemented throughout
</success_criteria>

<output>
After completion, create `.planning/phases/04-backend-api-enhancement/04-01-SUMMARY.md`
</output>

---
phase: 07-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Projects/PostCard.jsx
  - frontend/src/components/Projects/ProjectDetail.jsx
autonomous: true
requirements:
  - CLEANUP-01
must_haves:
  truths:
    - PostCard.jsx component file is deleted
    - PostCard import is removed from ProjectDetail.jsx
    - PostCard usage is removed from All Posts tab rendering
  artifacts:
    - path: frontend/src/components/Projects/PostCard.jsx
      provides: PostCard component
      min_lines: 0
      action: DELETE
    - path: frontend/src/components/Projects/ProjectDetail.jsx
      provides: ProjectDetail component without PostCard
      min_lines: 950
      contains: "no PostCard import or usage"
  key_links:
    - from: frontend/src/components/Projects/ProjectDetail.jsx
      to: frontend/src/components/Projects/PostCard.jsx
      via: import statement
      pattern: "import PostCard from"
---

<objective>
Remove PostCard component and its usage from the All Posts tab

Purpose: PostCard component was replaced by table view in Phase 6 and is no longer needed
Output: PostCard.jsx deleted, PostCard import and usage removed from ProjectDetail.jsx
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cleanup/07-CONTEXT.md
@frontend/src/components/Projects/PostCard.jsx
@frontend/src/components/Projects/ProjectDetail.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete PostCard.jsx component file</name>
  <files>frontend/src/components/Projects/PostCard.jsx</files>
  <read_first>
    - frontend/src/components/Projects/PostCard.jsx (verify file exists before deletion)
  </read_first>
  <action>Delete the entire PostCard.jsx file using the Bash tool with `rm frontend/src/components/Projects/PostCard.jsx`. This component is no longer needed as it was replaced by the table view in Phase 6.</action>
  <verify>
    <automated>test ! -f frontend/src/components/Projects/PostCard.jsx && echo "PostCard.jsx deleted successfully"</automated>
  </verify>
  <done>PostCard.jsx file is deleted and no longer exists in the codebase</done>
</task>

<task type="auto">
  <name>Task 2: Remove PostCard import from ProjectDetail.jsx</name>
  <files>frontend/src/components/Projects/ProjectDetail.jsx</files>
  <read_first>
    - frontend/src/components/Projects/ProjectDetail.jsx (read to find PostCard import line)
  </read_first>
  <action>Remove the PostCard import statement from line 7 of ProjectDetail.jsx. The line to remove is: `import PostCard from './PostCard'`. Use the Edit tool to remove this line entirely.</action>
  <verify>
    <automated>grep -c "import PostCard from" frontend/src/components/Projects/ProjectDetail.jsx | grep -q "^0$" && echo "PostCard import removed"</automated>
  </verify>
  <done>PostCard import statement is removed from ProjectDetail.jsx</done>
</task>

<task type="auto">
  <name>Task 3: Remove PostCard usage from All Posts tab rendering</name>
  <files>frontend/src/components/Projects/ProjectDetail.jsx</files>
  <read_first>
    - frontend/src/components/Projects/ProjectDetail.jsx (read lines 709-721 to find PostCard usage)
  </read_first>
  <action>Remove the PostCard component rendering from the All Posts tab. Specifically, remove lines 709-721 which contain the PostCard mapping:
```
<div className="stats-grid">
  {allPosts.map(post => (
    <PostCard
      key={post.id}
      post={post}
      onEdit={(post) => {
        if (post.wp_post_id && project.wp_site_url) {
          window.open(`${project.wp_site_url}/wp-admin/post.php?post=${post.wp_post_id}&action=edit`, '_blank')
        }
      }}
    />
  ))}
</div>
```
Replace this entire block with a placeholder comment: `<!-- PostCard removed - replaced by table view in Phase 6 -->`</action>
  <verify>
    <automated>grep -c "<PostCard" frontend/src/components/Projects/ProjectDetail.jsx | grep -q "^0$" && echo "PostCard usage removed"</automated>
  </verify>
  <done>PostCard component usage is removed from All Posts tab rendering</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only removes code, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | I | PostCard removal | accept | No information disclosure risk - removing unused code only |
| T-07-02 | D | PostCard removal | accept | No denial of service risk - removing unused code only |
| T-07-03 | E | PostCard removal | accept | No elevation risk - removing unused code only |
| T-07-04 | T | PostCard removal | accept | No tampering risk - removing unused code only |
| T-07-05 | R | PostCard removal | accept | No repudiation risk - removing unused code only |

**Note:** This is a cleanup phase with no new code, so all threats are accepted as low risk.
</threat_model>

<verification>
- PostCard.jsx file is deleted
- PostCard import is removed from ProjectDetail.jsx
- PostCard usage is removed from All Posts tab rendering
- No references to PostCard remain in ProjectDetail.jsx
</verification>

<success_criteria>
- PostCard.jsx component file no longer exists
- ProjectDetail.jsx has no PostCard import statement
- ProjectDetail.jsx has no PostCard component usage
- All Posts tab rendering no longer references PostCard
</success_criteria>

<output>
After completion, create `.planning/phases/07-cleanup/07-01-SUMMARY.md`
</output>

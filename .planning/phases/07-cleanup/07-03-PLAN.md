---
phase: 07-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - frontend/src/index.css
autonomous: true
requirements:
  - CLEANUP-02
must_haves:
  truths:
    - PostCard CSS classes are removed from index.css
    - Origin badge CSS classes are removed from index.css
    - Badge category and tag CSS classes are removed from index.css
  artifacts:
    - path: frontend/src/index.css
      provides: CSS without unused PostCard classes
      min_lines: 1200
      contains: "no post-card or origin-badge classes"
  key_links:
    - from: frontend/src/index.css
      to: N/A
      via: CSS classes
      pattern: "\.post-card|\.origin-badge|\.badge-category|\.badge-tag"
---

<objective>
Remove unused CSS classes from index.css

Purpose: CSS classes for PostCard component and origin badges are no longer needed after Phase 6 table view implementation
Output: Unused CSS classes removed from index.css
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cleanup/07-CONTEXT.md
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove post-card CSS classes</name>
  <files>frontend/src/index.css</files>
  <read_first>
    - frontend/src/index.css (read lines 1215-1316 to find post-card classes)
  </read_first>
  <action>Remove the following CSS class definitions that are no longer needed:
- Lines 1215-1220: `.post-card` class definition
- Lines 1222-1227: `.post-card-header` class definition
- Lines 1229-1251: `.origin-badge`, `.origin-badge.origin-tool`, `.origin-badge.origin-existing` class definitions
- Lines 1252-1264: `.post-card-title` class definition
- Lines 1265-1272: `.post-card-meta` class definition
- Lines 1273-1278: `.post-card-date` class definition
- Lines 1279-1298: `.post-card-url` class definition and hover state
- Lines 1299-1306: `.post-card-categories`, `.post-card-tags` class definitions

Use the Edit tool to remove these entire CSS class blocks. These classes are no longer used after the PostCard component was removed.</action>
  <verify>
    <automated>grep -c "\.post-card" frontend/src/index.css | grep -q "^0$" && echo "post-card CSS classes removed"</automated>
  </verify>
  <done>All post-card CSS classes are removed from index.css</done>
</task>

<task type="auto">
  <name>Task 2: Remove origin-badge CSS classes</name>
  <files>frontend/src/index.css</files>
  <read_first>
    - frontend/src/index.css (read lines 1229-1251 to find origin-badge classes)
  </read_first>
  <action>Remove the following CSS class definitions for origin badges:
- Lines 1229-1251: `.origin-badge`, `.origin-badge.origin-tool`, `.origin-badge.origin-existing` class definitions

Use the Edit tool to remove these entire CSS class blocks. These classes are no longer used after the PostCard component was removed.</action>
  <verify>
    <automated>grep -c "\.origin-badge" frontend/src/index.css | grep -q "^0$" && echo "origin-badge CSS classes removed"</automated>
  </verify>
  <done>All origin-badge CSS classes are removed from index.css</done>
</task>

<task type="auto">
  <name>Task 3: Remove badge-category and badge-tag CSS classes</name>
  <files>frontend/src/index.css</files>
  <read_first>
    - frontend/src/index.css (read lines 1307-1316 to find badge classes)
  </read_first>
  <action>Remove the following CSS class definitions for category and tag badges:
- Lines 1307-1316: `.badge-category`, `.badge-tag` class definitions

Use the Edit tool to remove these entire CSS class blocks. These classes are no longer used after the PostCard component was removed. Note: Keep the generic `.badge` class if it is used elsewhere in the application.</action>
  <verify>
    <automated>grep -c "\.badge-category" frontend/src/index.css | grep -q "^0$" && grep -c "\.badge-tag" frontend/src/index.css | grep -q "^0$" && echo "badge-category and badge-tag CSS classes removed"</automated>
  </verify>
  <done>badge-category and badge-tag CSS classes are removed from index.css</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only removes CSS, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | I | CSS class removal | accept | No information disclosure risk - removing unused CSS only |
| T-07-12 | D | CSS class removal | accept | No denial of service risk - removing unused CSS only |
| T-07-13 | E | CSS class removal | accept | No elevation risk - removing unused CSS only |
| T-07-14 | T | CSS class removal | accept | No tampering risk - removing unused CSS only |
| T-07-15 | R | CSS class removal | accept | No repudiation risk - removing unused CSS only |

**Note:** This is a cleanup phase with no new code, so all threats are accepted as low risk.
</threat_model>

<verification>
- post-card CSS classes are removed
- origin-badge CSS classes are removed
- badge-category and badge-tag CSS classes are removed
- No references to PostCard-related CSS classes remain
- Generic .badge class is preserved if used elsewhere
</verification>

<success_criteria>
- index.css has no .post-card classes
- index.css has no .post-card-header classes
- index.css has no .post-card-title classes
- index.css has no .post-card-meta classes
- index.css has no .post-card-date classes
- index.css has no .post-card-url classes
- index.css has no .post-card-categories classes
- index.css has no .post-card-tags classes
- index.css has no .origin-badge classes
- index.css has no .badge-category classes
- index.css has no .badge-tag classes
</success_criteria>

<output>
After completion, create `.planning/phases/07-cleanup/07-03-SUMMARY.md`
</output>

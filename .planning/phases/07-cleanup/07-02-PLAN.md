---
phase: 07-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - frontend/src/components/Projects/ProjectDetail.jsx
autonomous: true
requirements:
  - CLEANUP-03
  - CLEANUP-04
must_haves:
  truths:
    - Unused state variables are removed (allPosts, loadingAllPosts, allPostsError, page, hasMore, loadingMore)
    - loadAllPosts and loadMorePosts functions are removed
    - Infinite scroll event listener and related useEffect hooks are removed
    - "Loading more posts..." and "No more posts to load" messages are removed
  artifacts:
    - path: frontend/src/components/Projects/ProjectDetail.jsx
      provides: ProjectDetail component without infinite scroll
      min_lines: 900
      contains: "no infinite scroll logic or unused state"
  key_links:
    - from: frontend/src/components/Projects/ProjectDetail.jsx
      to: N/A
      via: state variables and functions
      pattern: "allPosts|loadingAllPosts|allPostsError|page|hasMore|loadingMore|loadAllPosts|loadMorePosts|handleScroll"
---

<objective>
Remove infinite scroll logic and unused state variables from ProjectDetail.jsx

Purpose: Infinite scroll was replaced by manual pagination in Phase 6, and related state variables are no longer needed
Output: Unused state variables, functions, and event listeners removed from ProjectDetail.jsx
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cleanup/07-CONTEXT.md
@frontend/src/components/Projects/ProjectDetail.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused state variables</name>
  <files>frontend/src/components/Projects/ProjectDetail.jsx</files>
  <read_first>
    - frontend/src/components/Projects/ProjectDetail.jsx (read lines 20-28 to find state variables)
  </read_first>
  <action>Remove the following unused state variable declarations from lines 20-28:
- Line 20: `const [allPosts, setAllPosts] = useState([])`
- Line 21: `const [loadingAllPosts, setLoadingAllPosts] = useState(false)`
- Line 22: `const [allPostsError, setAllPostsError] = useState(null)`
- Line 26: `const [page, setPage] = useState(1)`
- Line 27: `const [hasMore, setHasMore] = useState(true)`
- Line 28: `const [loadingMore, setLoadingMore] = useState(false)`

Use the Edit tool to remove these lines entirely. Keep the following state variables as they are still used:
- `statusFilter`, `sortBy`, `searchQuery` (used by table)
- `project`, `stats`, `posts`, `loading`, `tokenUsage` (used by other tabs)</action>
  <verify>
    <automated>grep -c "const \[allPosts, setAllPosts\]" frontend/src/components/Projects/ProjectDetail.jsx | grep -q "^0$" && grep -c "const \[loadingAllPosts, setLoadingAllPosts\]" frontend/src/components/Projects/ProjectDetail.jsx | grep -q "^0$" && grep -c "const \[allPostsError, setAllPostsError\]" frontend/src/components/Projects/ProjectDetail.jsx | grep -q "^0$" && grep -c "const \[page, setPage\]" frontend/src/components/Projects/ProjectDetail.jsx | grep -q "^0$" && grep -c "const \[hasMore, setHasMore\]" frontend/src/components/Projects/ProjectDetail.jsx | grep -q "^0$" && grep -c "const \[loadingMore, setLoadingMore\]" frontend/src/components/Projects/ProjectDetail.jsx | grep -q "^0$" && echo "All unused state variables removed"</automated>
  </verify>
  <done>All unused state variables are removed from ProjectDetail.jsx</done>
</task>

<task type="auto">
  <name>Task 2: Remove loadAllPosts and loadMorePosts functions</name>
  <files>frontend/src/components/Projects/ProjectDetail.jsx</files>
  <read_first>
    - frontend/src/components/Projects/ProjectDetail.jsx (read lines 275-331 to find functions)
  </read_first>
  <action>Remove the following function definitions:
- Lines 275-324: `const loadAllPosts = async (pageNum = 1, reset = false) => { ... }`
- Lines 326-331: `const loadMorePosts = async () => { ... }`

Use the Edit tool to remove these entire function definitions. These functions are no longer needed as the table view uses getSitePosts directly.</action>
  <verify>
    <automated>grep -c "const loadAllPosts" frontend/src/components/Projects/ProjectDetail.jsx | grep -q "^0$" && grep -c "const loadMorePosts" frontend/src/components/Projects/ProjectDetail.jsx | grep -q "^0$" && echo "loadAllPosts and loadMorePosts functions removed"</automated>
  </verify>
  <done>loadAllPosts and loadMorePosts functions are removed from ProjectDetail.jsx</done>
</task>

<task type="auto">
  <name>Task 3: Remove infinite scroll event listener and related useEffect hooks</name>
  <files>frontend/src/components/Projects/ProjectDetail.jsx</files>
  <read_first>
    - frontend/src/components/Projects/ProjectDetail.jsx (read lines 182-214 to find useEffect hooks)
  </read_first>
  <action>Remove the following useEffect hooks that are related to infinite scroll:
- Lines 182-186: useEffect that calls loadAllPosts when page changes
- Lines 188-205: useEffect with handleScroll event listener for infinite scroll
- Lines 207-214: useEffect that resets pagination when filter/sort/search changes

Use the Edit tool to remove these entire useEffect blocks. These hooks are no longer needed as the table view uses manual pagination.</action>
  <verify>
    <automated>grep -c "handleScroll" frontend/src/components/Projects/ProjectDetail.jsx | grep -q "^0$" && echo "Infinite scroll event listener removed"</automated>
  </verify>
  <done>Infinite scroll event listener and related useEffect hooks are removed from ProjectDetail.jsx</done>
</task>

<task type="auto">
  <name>Task 4: Remove "Loading more posts..." and "No more posts to load" messages</name>
  <files>frontend/src/components/Projects/ProjectDetail.jsx</files>
  <read_first>
    - frontend/src/components/Projects/ProjectDetail.jsx (read lines 722-732 to find messages)
  </read_first>
  <action>Remove the following UI elements that display infinite scroll status:
- Lines 722-727: "Loading more posts..." message with loading spinner
- Lines 728-732: "No more posts to load" message

Use the Edit tool to remove these entire blocks. These messages are no longer needed as the table view uses manual pagination.</action>
  <verify>
    <automated>grep -c "Loading more posts" frontend/src/components/Projects/ProjectDetail.jsx | grep -q "^0$" && grep -c "No more posts to load" frontend/src/components/Projects/ProjectDetail.jsx | grep -q "^0$" && echo "Infinite scroll messages removed"</automated>
  </verify>
  <done>"Loading more posts..." and "No more posts to load" messages are removed from ProjectDetail.jsx</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only removes code, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | I | State variable removal | accept | No information disclosure risk - removing unused code only |
| T-07-07 | D | Function removal | accept | No denial of service risk - removing unused code only |
| T-07-08 | E | Event listener removal | accept | No elevation risk - removing unused code only |
| T-07-09 | T | useEffect removal | accept | No tampering risk - removing unused code only |
| T-07-10 | R | Message removal | accept | No repudiation risk - removing unused code only |

**Note:** This is a cleanup phase with no new code, so all threats are accepted as low risk.
</threat_model>

<verification>
- Unused state variables are removed
- loadAllPosts and loadMorePosts functions are removed
- Infinite scroll event listener is removed
- Related useEffect hooks are removed
- "Loading more posts..." and "No more posts to load" messages are removed
- No references to infinite scroll logic remain
</verification>

<success_criteria>
- ProjectDetail.jsx has no allPosts, loadingAllPosts, allPostsError, page, hasMore, or loadingMore state variables
- ProjectDetail.jsx has no loadAllPosts or loadMorePosts functions
- ProjectDetail.jsx has no handleScroll event listener
- ProjectDetail.jsx has no useEffect hooks related to infinite scroll
- ProjectDetail.jsx has no "Loading more posts..." or "No more posts to load" messages
</success_criteria>

<output>
After completion, create `.planning/phases/07-cleanup/07-02-SUMMARY.md`
</output>

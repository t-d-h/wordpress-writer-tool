---
phase: 19
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/services/user_service.py]
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, IDEMP-01, IDEMP-02, IDEMP-03]
user_setup: []

must_haves:
  truths:
    - "System creates admin account on first application startup using INIT_USER and INIT_PASSWORD"
    - "System prevents INIT_USER from being 'admin' to avoid username conflict"
    - "System checks if admin account already exists before creating (idempotent)"
    - "System handles container restarts gracefully without duplicate key errors"
    - "System logs when admin account is created or when it already exists"
    - "System uses hash_password() to hash INIT_PASSWORD before storing"
  artifacts:
    - path: "backend/app/services/user_service.py"
      provides: "create_admin_account() function with INIT_USER/INIT_PASSWORD support"
      min_lines: 30
      contains: "async def create_admin_account()"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/services/user_service.py"
      via: "startup event calls create_admin_account()"
      pattern: "create_admin_account\\(\\)"
    - from: "backend/app/services/user_service.py"
      to: "backend/app/config.py"
      via: "reads INIT_USER and INIT_PASSWORD from settings"
      pattern: "settings\\.INIT_USER|settings\\.INIT_PASSWORD"
    - from: "backend/app/services/user_service.py"
      to: "backend/app/database.py"
      via: "queries users_col for existing admin account"
      pattern: "users_col\\.find_one"
    - from: "backend/app/services/user_service.py"
      to: "backend/app/services/auth_service.py"
      via: "uses hash_password() to hash INIT_PASSWORD"
      pattern: "hash_password"
---

<objective>
Update create_admin_account() function to use INIT_USER and INIT_PASSWORD environment variables with idempotent behavior, username conflict prevention, and comprehensive logging.

Purpose: Enable initial admin account creation on first startup using environment variables, while preventing username conflicts with the existing 'admin' account and handling container restarts gracefully.

Output: Updated create_admin_account() function that creates admin account using INIT_USER/INIT_PASSWORD, prevents 'admin' username, checks for existing accounts, and logs all scenarios.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-admin-account-creation/19-CONTEXT.md
@.planning/phases/18-environment-variable-validation/18-01-SUMMARY.md

# Existing code context

From backend/app/services/user_service.py:
```python
async def create_admin_account():
    """Create admin account on first startup if it doesn't exist."""
    existing_admin = await users_col.find_one({"username": "admin"})
    if existing_admin:
        return

    admin_data = {
        "username": "admin",
        "password_hash": hash_password(settings.ADMIN_PASSWORD),
        "role": "admin",
        "created_at": datetime.now(timezone.utc).isoformat(),
        "last_login_at": None,
    }
    await users_col.insert_one(admin_data)
```

From backend/app/config.py:
```python
INIT_USER: str = os.getenv("INIT_USER")
INIT_PASSWORD: str = os.getenv("INIT_PASSWORD")
```

From backend/app/services/auth_service.py:
```python
def hash_password(password: str) -> str:
    """Hash password using Argon2id."""
    return ph.hash(password)
```

From backend/app/database.py:
```python
users_col = db["users"]
await users_col.create_index([("username", 1)], unique=True)
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update create_admin_account() to use INIT_USER and INIT_PASSWORD</name>
  <files>backend/app/services/user_service.py</files>
  <action>Update the create_admin_account() function in backend/app/services/user_service.py:

1. Add import logging at top of file (if not already present)
2. Initialize logger: `logger = logging.getLogger(__name__)`
3. Update function to use INIT_USER and INIT_PASSWORD from settings instead of hardcoded 'admin' and ADMIN_PASSWORD
4. Add validation to prevent INIT_USER from being 'admin' (per D-01, D-02, D-03):
   - Check if settings.INIT_USER == 'admin'
   - If true, log error with logger.error() and raise ValueError with message: "INIT_USER cannot be 'admin' — this username is reserved for the existing admin account"
5. Update existing admin check to use settings.INIT_USER instead of hardcoded 'admin'
6. Add logging for all scenarios (per D-13, D-14, D-15):
   - Log error when INIT_USER is 'admin' (logger.error() before raising ValueError)
   - Log info when admin account is created (logger.info())
   - Log info when admin account already exists and creation is skipped (logger.info())
7. Keep the function name and signature the same (per D-07, D-08)
8. Use existing hash_password() function from auth_service to hash INIT_PASSWORD (per D-16)
9. Store password_hash in users collection (not plain text password) (per D-17)
10. User schema includes: username, password_hash, role, created_at, last_login_at (per D-18)

The function should:
- Check if INIT_USER is 'admin' → log error and raise ValueError
- Check if admin account already exists (query users_col by username) → log info and return
- Create admin account with INIT_USER and hashed INIT_PASSWORD → log info and insert

Do NOT change the function name or add new parameters. The function is called from main.py startup event and must remain compatible.</action>
  <verify>
    <automated>grep -n "async def create_admin_account" backend/app/services/user_service.py && grep -n "settings.INIT_USER" backend/app/services/user_service.py && grep -n "settings.INIT_PASSWORD" backend/app/services/user_service.py && grep -n "INIT_USER cannot be 'admin'" backend/app/services/user_service.py && grep -n "logger.error" backend/app/services/user_service.py && grep -n "logger.info" backend/app/services/user_service.py</automated>
  </verify>
  <done>create_admin_account() function updated to use INIT_USER and INIT_PASSWORD, prevents 'admin' username, checks for existing accounts, and logs all scenarios</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment→config | INIT_USER and INIT_PASSWORD from environment variables (untrusted input) |
| config→service | Settings object passed to create_admin_account() |
| service→database | User data stored in MongoDB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | INIT_USER environment variable | accept | Low-value target, network isolation provides protection. Validation prevents 'admin' username conflict. |
| T-19-02 | Tampering | INIT_PASSWORD environment variable | accept | Low-value target, network isolation provides protection. Password is hashed with Argon2id before storage. |
| T-19-03 | Information Disclosure | Error messages | accept | Error messages do not contain sensitive information, only variable names and generic conflict messages. |
| T-19-04 | Denial of Service | Duplicate key error on container restart | mitigate | Idempotent behavior checks if account exists before creating, preventing duplicate key errors. Unique index on username provides database-level protection. |
| T-19-05 | Spoofing | Username conflict with existing 'admin' account | mitigate | Validation prevents INIT_USER from being 'admin', raising ValueError with clear error message before attempting to create account. |
</threat_model>

<verification>
## Phase Verification

1. **Code Review**: Verify create_admin_account() function uses INIT_USER and INIT_PASSWORD from settings
2. **Validation Check**: Verify function prevents INIT_USER from being 'admin' with clear error message
3. **Idempotent Check**: Verify function checks if admin account already exists before creating
4. **Logging Check**: Verify function logs all scenarios (error on 'admin', info on create, info on skip)
5. **Password Hashing**: Verify function uses hash_password() to hash INIT_PASSWORD before storage
6. **Schema Check**: Verify user data includes username, password_hash, role, created_at, last_login_at
7. **Integration Check**: Verify function is called from main.py startup event (no changes needed)
8. **Requirements Check**: Verify all requirements (ADMIN-01, ADMIN-02, ADMIN-03, IDEMP-01, IDEMP-02, IDEMP-03) are met
</verification>

<success_criteria>
1. create_admin_account() function uses INIT_USER and INIT_PASSWORD from settings
2. Function prevents INIT_USER from being 'admin' with clear error message
3. Function checks if admin account already exists before creating (idempotent)
4. Function handles container restarts gracefully without duplicate key errors
5. Function logs when admin account is created or when it already exists
6. Function uses hash_password() to hash INIT_PASSWORD before storing
7. User data includes username, password_hash, role, created_at, last_login_at
8. All requirements (ADMIN-01, ADMIN-02, ADMIN-03, IDEMP-01, IDEMP-02, IDEMP-03) are met
</success_criteria>

<output>
After completion, create `.planning/phases/19-admin-account-creation/19-01-SUMMARY.md`
</output>

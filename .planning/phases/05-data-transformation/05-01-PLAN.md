---
phase: 05-data-transformation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/wp_service.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
user_setup: []
must_haves:
  truths:
    - WordPress REST API response is transformed to table format with all required fields
    - Nested categories and tags from _embedded['wp:term'] are extracted correctly
    - Dates are formatted for display in table
    - Edit URLs are generated for WordPress admin
  artifacts:
    - path: backend/app/services/wp_service.py
      provides: Enhanced get_wp_posts with data transformation
      exports: ["get_wp_posts"]
      min_lines: 350
  key_links:
    - from: backend/app/services/wp_service.py
      to: frontend/src/components/AllPosts.jsx
      via: "Transformed data consumption"
      pattern: "get_wp_posts"
    - from: backend/app/services/wp_service.py
      to: backend/app/routers/wp_sites.py
      via: "Service layer transformation"
      pattern: "get_wp_posts"
---

<objective>
Transform WordPress REST API responses into table-ready format in backend service layer, including extracting nested categories/tags, formatting dates, and generating edit URLs.

Purpose: Simplify frontend by having backend return properly formatted data for table display, eliminating transformation logic from frontend components.

Output: Enhanced get_wp_posts function that returns transformed data with all table fields ready for display.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/bin/gsd-tools.cjs
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-transformation/05-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@backend/app/services/wp_service.py
@frontend/src/components/AllPosts.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add transformation helper functions to wp_service.py</name>
  <files>backend/app/services/wp_service.py</files>
  <action>Add transformation helper functions to backend/app/services/wp_service.py:

1. Add _format_date() helper function:
   - Takes WordPress date string (ISO 8601 format)
   - Returns formatted date string in DD MMMM YYYY format (e.g., "14 April 2026")
   - Handles invalid dates gracefully (returns empty string)

2. Add _extract_categories() helper function:
   - Takes _embedded['wp:term'][0] array from WordPress response
   - Extracts category names into simple array of strings
   - Returns empty array if _embedded is missing or null

3. Add _extract_tags() helper function:
   - Takes _embedded['wp:term'][1] array from WordPress response
   - Extracts tag names into simple array of strings
   - Returns empty array if _embedded is missing or null

4. Add _generate_edit_url() helper function:
   - Takes WordPress site URL and post ID
   - Constructs full edit URL for WordPress admin
   - Returns empty string if parameters are invalid

Place helper functions at module level in wp_service.py, before or after existing functions.
</action>
  <verify>
    <automated>grep -q "_format_date\|_extract_categories\|_extract_tags\|_generate_edit_url" backend/app/services/wp_service.py && echo "Helper functions found"</automated>
  </verify>
  <done>Transformation helper functions added to wp_service.py</done>
</task>

<task type="auto">
  <name>Task 2: Transform posts data in get_wp_posts function</name>
  <files>backend/app/services/wp_service.py</files>
  <action>Update get_wp_posts() function in backend/app/services/wp_service.py to transform posts data:

1. After fetching posts from WordPress API, transform each post:
   - Extract categories using _extract_categories(post._embedded)
   - Extract tags using _extract_tags(post._embedded)
   - Format date using _format_date(post.date)
   - Generate edit URL using _generate_edit_url(wp_site['url'], post.id)

2. Add transformed fields to each post object:
   - categories: array of category names
   - tags: array of tag names
   - formatted_date: formatted date string
   - edit_url: full edit URL string

3. Keep original WordPress fields intact for backward compatibility:
   - title.rendered, link, status, etc. remain unchanged
   - New fields are added alongside existing fields

4. Return transformed posts in response dict along with total count.

Per D-01 decision: Transformation happens in backend service layer.
Per D-09 decision: Search always hits WordPress API (not cached data).
</action>
  <verify>
    <automated>grep -A 5 "categories.*_extract_categories\|tags.*_extract_tags\|formatted_date.*_format_date\|edit_url.*_generate_edit_url" backend/app/services/wp_service.py | head -20</automated>
  </verify>
  <done>Posts data transformed with categories, tags, formatted date, and edit URL</done>
</task>

<task type="auto">
  <name>Task 3: Add error handling for missing or invalid data</name>
  <files>backend/app/services/wp_service.py</files>
  <action>Add comprehensive error handling for missing or invalid data in transformation:

1. In _extract_categories():
   - Check if _embedded exists and is not None
   - Check if _embedded['wp:term'] exists and has at least 2 elements
   - Check if _embedded['wp:term'][0] exists and is not None
   - Return empty array if any check fails

2. In _extract_tags():
   - Check if _embedded exists and is not None
   - Check if _embedded['wp:term'] exists and has at least 2 elements
   - Check if _embedded['wp:term'][1] exists and is not None
   - Return empty array if any check fails

3. In _format_date():
   - Check if date string is not None and not empty
   - Try to parse date string using datetime.fromisoformat()
   - If parsing fails, return empty string
   - Format valid date as DD MMMM YYYY using strftime()

4. In _generate_edit_url():
   - Check if site_url is not None and not empty
   - Check if post_id is not None and is valid
   - Construct URL with proper format
   - Return empty string if any check fails

5. Wrap all transformation logic in try/except blocks
   - Log errors using print() for debugging
   - Return empty string or empty array on error

Per D-14 through D-18 decisions: Graceful degradation for missing data.
</action>
  <verify>
    <automated>grep -E "try:|except.*Exception|print.*Error" backend/app/services/wp_service.py | wc -l</automated>
  </verify>
  <done>Error handling added for missing or invalid data</done>
</task>

<task type="auto">
  <name>Task 4: Verify transformation output structure</name>
  <files>backend/app/services/wp_service.py</files>
  <action>Verify transformation output structure matches frontend expectations:

1. Ensure each transformed post has these fields:
   - categories: array of strings (category names)
   - tags: array of strings (tag names)
   - formatted_date: string in DD MMMM YYYY format
   - edit_url: full URL string for WordPress admin

2. Ensure response structure remains compatible:
   - Response dict with 'posts' list and 'total' count
   - Each post retains original WordPress fields
   - New fields are added, not replacing existing fields

3. Test with sample data:
   - Post with categories and tags
   - Post with missing _embedded
   - Post with invalid date
   - Post with missing post_id

Per D-19 decision: Transformed response structure matches frontend table expectations.
Per D-20 through D-22 decisions: Data structure includes all required fields.
</action>
  <verify>
    <automated>grep -A 10 "return {" backend/app/services/wp_service.py | grep -E "posts.*total|categories|tags|formatted_date|edit_url"</automated>
  </verify>
  <done>Transformation output structure verified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WordPress API → Backend | Untrusted external API responses cross here |
| Backend → Frontend | Transformed data (potentially malformed) crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I | get_wp_posts() | mitigate | Validate _embedded structure before extraction |
| T-05-02 | I | _format_date() | mitigate | Validate date format before parsing |
| T-05-03 | I | _generate_edit_url() | mitigate | Validate site_url and post_id before constructing URL |
| T-05-04 | D | Transformation errors | accept | Graceful degradation with empty strings/arrays |
| T-05-05 | T | get_wp_posts() | accept | Transformation errors logged, not critical |

</threat_model>

<verification>
- Transformation helper functions exist in wp_service.py
- Posts data is transformed with categories, tags, formatted date, and edit URL
- Error handling covers all missing or invalid data scenarios
- Transformation output structure matches frontend expectations
- Response structure remains backward compatible
</verification>

<success_criteria>
- WordPress REST API response is transformed to table format with all required fields
- Nested categories and tags from _embedded['wp:term'] are extracted correctly
- Dates are formatted for display in table (DD MMMM YYYY format)
- Edit URLs are generated for WordPress admin
- Error handling provides graceful degradation for missing data
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-transformation/05-01-SUMMARY.md`
</output>

# Plan: Frontend API and State - Token Usage Display

**Phase:** 01 - Token Usage Display
**Wave:** 2
**Status:** Not started

**type:** implementation
**autonomous:** false
**depends_on:** 01-01
**files_modified:** frontend/src/api/client.js, frontend/src/components/Projects/ProjectDetail.jsx

## Objective

Implement frontend API client method and state management for token usage data in ProjectDetail component.

## Requirements

- TOKEN-01: User can view token usage breakdown in Project general tab above statistics section
- TOKEN-06: Token usage display is always visible when viewing project details
- UX-01: Token usage display is visually distinct from existing statistics

## must_haves

### truths
- User can view token usage breakdown in Project general tab above statistics section
- System displays token usage breakdown by post type (research, outline, content, thumbnail)
- System shows total input tokens and total output tokens across all post types
- System includes deleted posts in token usage calculations
- Token usage display loads within 1 second for projects with <100 posts

### artifacts
- `frontend/src/api/client.js` - API client method for fetching token usage
- `frontend/src/components/Projects/ProjectDetail.jsx` - Token usage state management

### key_links
- API client method → Backend stats endpoint
- State management → TokenUsageCard component
- Loading state → User feedback during data fetch

## Tasks

<task>
<name>Add API client method</name>
<files>
- frontend/src/api/client.js
</files>

<action>
Add API client method to fetch token usage data from backend:

1. Read existing API client methods to understand pattern
2. Add `getProjectTokenUsage(projectId)` method
3. Method should call `/api/projects/{id}/stats` endpoint
4. Extract token usage data from response
5. Return token usage object with breakdown by type and totals
6. Handle errors gracefully with meaningful error messages
7. Test method with sample project ID
</action>

<verify>
<manual>
- API client method successfully fetches token usage data
- Method handles errors gracefully
- Returns properly formatted token usage object
- Error messages are meaningful
</manual>
</verify>

<done>
- API client method added and tested
- Error handling implemented
</done>
</task>

<task>
<name>Add token usage state</name>
<files>
- frontend/src/components/Projects/ProjectDetail.jsx
</files>

<action>
Add state management for token usage data in ProjectDetail component:

1. Read current state structure in ProjectDetail component
2. Add `tokenUsage` state variable initialized to null
3. Add `loadingTokenUsage` state variable for loading state
4. Add `tokenUsageError` state variable for error handling
5. Load token usage data in `useEffect` hook when project loads
6. Call `getProjectTokenUsage(id)` API method
7. Handle loading, error, and success states appropriately
8. Ensure token usage data loads in parallel with existing stats
9. Test state updates with mock data
</action>

<verify>
<manual>
- Token usage state properly initialized
- Loading state shows during data fetch
- Error state shows if fetch fails
- Token usage data populated on success
- State updates don't cause re-render loops
</manual>
</verify>

<done>
- Token usage state properly initialized
- Loading and error states implemented
- Data fetches successfully on project load
</done>
</task>

<task>
<files>
- frontend/src/components/Projects/ProjectDetail.jsx
</files>

<action>
Add state management for token usage data in ProjectDetail component:

1. Read current state structure in ProjectDetail component
2. Add `tokenUsage` state variable initialized to null
3. Add `loadingTokenUsage` state variable for loading state
4. Add `tokenUsageError` state variable for error handling
5. Load token usage data in `useEffect` hook when project loads
6. Call `getProjectTokenUsage(id)` API method
7. Handle loading, error, and success states appropriately
8. Ensure token usage data loads in parallel with existing stats
9. Test state updates with mock data
</action>

<verify>
<automated>
Browser devtools check for tokenUsage state
</automated>
<manual>
- Token usage state properly initialized
- Loading state shows during data fetch
- Error state shows if fetch fails
- Token usage data populated on success
- State updates don't cause re-render loops
</manual>
</verify>

<done>
- Token usage state properly initialized
- Loading and error states implemented
- Data fetches successfully on project load
</done>
</task>

## Files Modified

- `frontend/src/api/client.js` - Add API client method
- `frontend/src/components/Projects/ProjectDetail.jsx` - Add token usage state

## Dependencies

- Plan 01-01 (Backend Foundation) - API endpoint must be available first

## Success Criteria

1. API client method successfully fetches token usage data
2. Token usage state properly initialized and managed
3. Loading and error states display correctly
4. Token usage data updates when project loads
5. No re-render loops or performance issues

---
phase: 17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/config.py]
autonomous: true
requirements: [CONFIG-01, CONFIG-02, CONFIG-03]
user_setup: []
must_haves:
  truths:
    - "config.py includes INIT_USER field"
    - "config.py includes INIT_PASSWORD field"
    - "System validates INIT_USER and INIT_PASSWORD on startup"
    - "System fails startup if INIT_USER or INIT_PASSWORD are missing or empty"
    - "Validation raises ValueError with clear error messages"
  artifacts:
    - path: "backend/app/config.py"
      provides: "Configuration with INIT_USER and INIT_PASSWORD fields and validation"
      min_lines: 25
  key_links:
    - from: "backend/app/config.py"
      to: "backend/app/main.py"
      via: "Settings instantiation"
      pattern: "settings = Settings()"
---

<objective>
Add INIT_USER and INIT_PASSWORD fields to config.py with validation on startup

Purpose: Provide configuration infrastructure for initial admin account setup, ensuring required credentials are present before application starts
Output: Updated config.py with INIT_USER and INIT_PASSWORD fields and startup validation
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-configuration-layer/17-CONTEXT.md
@.planning/REQUIREMENTS.md
@backend/app/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add INIT_USER and INIT_PASSWORD fields to Settings class</name>
  <files>backend/app/config.py</files>
  <action>
    Add two new fields to the Settings class in backend/app/config.py:
    1. INIT_USER: str = os.getenv("INIT_USER")
    2. INIT_PASSWORD: str = os.getenv("INIT_PASSWORD")

    These fields must NOT have default values (per D-02). They must be set via environment variables only.

    Place these fields after the existing ADMIN_PASSWORD field (line 15) to maintain logical grouping of authentication-related configuration.
  </action>
  <verify>
    <automated>grep -q "INIT_USER: str = os.getenv" backend/app/config.py && grep -q "INIT_PASSWORD: str = os.getenv" backend/app/config.py</automated>
  </verify>
  <done>INIT_USER and INIT_PASSWORD fields added to Settings class with no default values</done>
</task>

<task type="auto">
  <name>Task 2: Add validation method to Settings class</name>
  <files>backend/app/config.py</files>
  <action>
    Add a validate() method to the Settings class that checks INIT_USER and INIT_PASSWORD are not empty.

    The method should:
    1. Check if INIT_USER is None or empty string, raise ValueError with message "INIT_USER environment variable is required and cannot be empty"
    2. Check if INIT_PASSWORD is None or empty string, raise ValueError with message "INIT_PASSWORD environment variable is required and cannot be empty"
    3. Return True if both fields are valid

    Place this method after all field definitions, before the settings instantiation (before line 18).
  </action>
  <verify>
    <automated>grep -q "def validate(self)" backend/app/config.py && grep -q "INIT_USER environment variable is required" backend/app/config.py && grep -q "INIT_PASSWORD environment variable is required" backend/app/config.py</automated>
  </verify>
  <done>Validation method added to Settings class with clear error messages</done>
</task>

<task type="auto">
  <name>Task 3: Call validation on Settings instantiation</name>
  <files>backend/app/config.py</files>
  <action>
    Update the settings instantiation (line 18) to call validate() immediately after creating the Settings instance.

    Change from:
    settings = Settings()

    To:
    settings = Settings()
    settings.validate()

    This ensures validation runs immediately on startup (per D-06), failing fast if required fields are missing.
  </action>
  <verify>
    <automated>grep -A1 "settings = Settings()" backend/app/config.py | grep -q "settings.validate()"</automated>
  </verify>
  <done>Validation called on Settings instantiation, system fails fast if INIT_USER or INIT_PASSWORD are missing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment → config.py | Untrusted environment variables cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | T (Tampering) | INIT_USER, INIT_PASSWORD environment variables | accept | Low-value target (local config), no PII, network isolation provides protection |
| T-17-02 | I (Information Disclosure) | Error messages revealing system details | mitigate | Error messages are generic ("required and cannot be empty"), no sensitive info leaked |
| T-17-03 | D (Denial of Service) | Missing environment variables causing startup failure | accept | This is intended behavior (fail-fast), not a vulnerability |
</threat_model>

<verification>
- [ ] config.py includes INIT_USER field with no default value
- [ ] config.py includes INIT_PASSWORD field with no default value
- [ ] config.py includes validate() method that checks both fields
- [ ] validate() raises ValueError with clear messages for missing/empty fields
- [ ] settings.validate() is called on instantiation
- [ ] System fails startup if INIT_USER or INIT_PASSWORD are missing or empty
</verification>

<success_criteria>
- [ ] INIT_USER field added to Settings class (no default value)
- [ ] INIT_PASSWORD field added to Settings class (no default value)
- [ ] validate() method checks both fields for None or empty string
- [ ] validate() raises ValueError with clear error messages
- [ ] settings.validate() called on instantiation
- [ ] System fails fast on startup if required fields are missing
</success_criteria>

<output>
After completion, create `.planning/phases/17-configuration-layer/17-01-SUMMARY.md`
</output>

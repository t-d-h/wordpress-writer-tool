---
phase: 03-all-posts-tab-ui
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Projects/PostCard.jsx
  - frontend/src/index.css
autonomous: true
gap_closure: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "System displays post URL (POSTS-06)"
  artifacts:
    - path: "frontend/src/components/Projects/PostCard.jsx"
      provides: "Post URL display in PostCard"
      min_lines: 55
    - path: "frontend/src/index.css"
      provides: "CSS styling for post URL"
      contains: "post-card-url"
  key_links:
    - from: "frontend/src/components/Projects/PostCard.jsx"
      to: "post.wp_post_url"
      via: "Render URL as clickable link"
      pattern: "post\\.wp_post_url"
---

<objective>
Display post URL in PostCard component so users can view the published post URL.

Purpose: Close gap POSTS-06 where wp_post_url is returned by backend API but not displayed in PostCard component.
Output: PostCard component displays post URL as a clickable link or metadata field.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-all-posts-tab-ui/03-VERIFICATION.md
@.planning/phases/03-all-posts-tab-ui/03-01-SUMMARY.md
@frontend/src/components/Projects/PostCard.jsx
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add post URL display to PostCard component</name>
  <files>frontend/src/components/Projects/PostCard.jsx</files>
  <action>
    Add post URL display to PostCard component:
    1. After the post-card-meta div (line 37), add a new div for post URL
    2. Render post.wp_post_url as a clickable link if it exists
    3. Use a small, subtle styling for the URL (e.g., gray text, smaller font)
    4. Add target="_blank" and rel="noopener noreferrer" to the link for security
    5. Handle null/undefined wp_post_url gracefully (don't render if missing)

    Example structure to add after line 38:
    ```jsx
    {post.wp_post_url && (
      <div className="post-card-url">
        <a href={post.wp_post_url} target="_blank" rel="noopener noreferrer">
          {post.wp_post_url}
        </a>
      </div>
    )}
    ```

    This follows the existing pattern in PostCard for conditional rendering (see line 39 for wp_post_id check).
  </action>
  <verify>
    <automated>grep -n "post-card-url" frontend/src/components/Projects/PostCard.jsx</automated>
  </verify>
  <done>PostCard component displays post URL as a clickable link when wp_post_url is available</done>
</task>

<task type="auto">
  <name>Task 2: Add CSS styling for post URL</name>
  <files>frontend/src/index.css</files>
  <action>
    Add CSS styling for post-card-url class:
    1. Find the post-card section in index.css (around line 1193-1255)
    2. Add a new .post-card-url class after .post-card-meta
    3. Style it with:
       - Small font size (0.85em or similar)
       - Gray color (#666 or similar)
       - Truncation for long URLs (text-overflow: ellipsis, white-space: nowrap, overflow: hidden)
       - Margin-top: 8px to separate from date
       - Link styling: remove underline, add hover effect

    Example CSS to add:
    ```css
    .post-card-url {
      margin-top: 8px;
      font-size: 0.85em;
      color: #666;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    .post-card-url a {
      color: #666;
      text-decoration: none;
      transition: color 0.2s;
    }

    .post-card-url a:hover {
      color: #007bff;
      text-decoration: underline;
    }
    ```

    This follows the existing CSS patterns in index.css for post-card styling.
  </action>
  <verify>
    <automated>grep -n "post-card-url" frontend/src/index.css</automated>
  </verify>
  <done>CSS styling for post URL is added with proper truncation and hover effects</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Post URL from backend API response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | T | PostCard URL link | mitigate | Add rel="noopener noreferrer" to prevent tabnabbing attacks on external links |
| T-03-06-02 | I | Post URL display | accept | URL is from trusted WordPress site configured by user, low XSS risk |
</threat_model>

<verification>
1. Check that PostCard.jsx renders post URL when wp_post_url exists
2. Check that CSS styling is applied correctly
3. Verify that link opens in new tab with security attributes
4. Test that missing wp_post_url doesn't break rendering
</verification>

<success_criteria>
- PostCard component displays post URL as a clickable link
- URL is styled subtly and truncates for long URLs
- Link opens in new tab with rel="noopener noreferrer"
- Missing wp_post_url doesn't cause rendering errors
- Gap POSTS-06 is closed (verified by re-running verification)
</success_criteria>

<output>
After completion, create `.planning/phases/03-all-posts-tab-ui/03-06-SUMMARY.md`
</output>

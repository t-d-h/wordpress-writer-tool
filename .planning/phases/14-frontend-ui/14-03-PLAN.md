---
phase: 14-frontend-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/components/Posts/PostView.jsx]
autonomous: true
requirements: [LANG-02]
user_setup: []

must_haves:
  truths:
    - "Language badge is displayed in post detail view"
    - "Language badge uses same color coding as post list (green for Vietnamese, blue for English)"
    - "Language badge shows language name"
    - "Language badge is visible in the page header area"
  artifacts:
    - path: "frontend/src/components/Posts/PostView.jsx"
      provides: "Language badge display in post detail view"
      min_lines: 520
  key_links:
    - from: "frontend/src/components/Posts/PostView.jsx"
      to: "post.language"
      via: "LanguageBadge component prop"
      pattern: "LanguageBadge.*language"
    - from: "LanguageBadge component"
      to: "CSS classes"
      via: "Conditional class based on language value"
      pattern: "badge-vietnamese|badge-english"
---

<objective>
Add language badge display to the post detail view in PostView component.

Purpose: Allow users to identify the language of the post they are viewing with a color-coded badge.
Output: Updated post detail view with language badge in page header area.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-backend-foundation/12-02-SUMMARY.md
@.planning/phases/12-backend-foundation/12-03-SUMMARY.md

# Backend API returns language field in post response (from Phase 12)
# PostResponse model includes language field
# GET /api/posts/{id} returns language field
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add language badge to post detail view</name>
  <files>frontend/src/components/Posts/PostView.jsx</files>
  <action>Create a LanguageBadge component function at the bottom of PostView.jsx (similar to the one created in ProjectDetail.jsx). The component should accept a language prop ("vietnamese" or "english") and render a badge with color coding: green background for Vietnamese, blue background for English. Use CSS classes like "language-badge badge-vietnamese" and "language-badge badge-english" for styling. Display the language name: "Tiếng Việt" for Vietnamese, "English" for English. Add PropTypes validation for the language prop. Then, add the LanguageBadge component to the page header area (after the status badge, before the action buttons), passing post.language as the prop. Handle missing language field gracefully by defaulting to "English" (using post.language || 'english').</action>
  <verify>LanguageBadge component exists in PostView.jsx, language badge is displayed in page header with correct color and language name</verify>
  <done>Language badge added to post detail view with color-coded display for Vietnamese and English</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→client | Language field from API response crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Tampering | Language display from API | accept | Backend controls language field, client only displays what API returns |
| T-14-06 | Information disclosure | Language badge reveals content language | accept | Language is not sensitive information, display is intentional feature |
</threat_model>

<verification>
- Language badge appears in post detail view page header
- Language badge displays correct color (green for Vietnamese, blue for English)
- Language badge displays correct language name
- Posts without language field default to English badge
- Badge styling matches design system and post list badges
</verification>

<success_criteria>
- Language badge is visible in post detail view
- Color coding matches post list (green for Vietnamese, blue for English)
- Language name is displayed correctly
- Missing language field is handled gracefully
- Badge is positioned appropriately in page header
</success_criteria>

<output>
After completion, create `.planning/phases/14-frontend-ui/14-03-SUMMARY.md`
</output>

---
phase: 14-frontend-ui
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/components/Projects/ProjectDetail.jsx]
autonomous: true
gap_closure: true
requirements: [LANG-06]
user_setup: []

must_haves:
  truths:
    - "Language selection persists when user navigates away and returns"
    - "Language selection stored in localStorage"
  artifacts:
    - path: "frontend/src/components/Projects/ProjectDetail.jsx"
      provides: "localStorage persistence for language selection"
      contains: "localStorage.getItem|localStorage.setItem"
  key_links:
    - from: "ProjectDetail.jsx component mount"
      to: "localStorage"
      via: "localStorage.getItem('languagePreference')"
      pattern: "localStorage.getItem.*language"
    - from: "Language selection onChange"
      to: "localStorage"
      via: "localStorage.setItem('languagePreference', value)"
      pattern: "localStorage.setItem.*language"
---

<objective>
Implement localStorage persistence for language selection in the Create Post form.

Purpose: Allow users' language preference to persist across page navigation and modal open/close cycles, improving user experience by remembering their last selection.
Output: Updated ProjectDetail.jsx with localStorage read/write for language preference.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/14-frontend-ui/14-01-SUMMARY.md
@.planning/phases/14-frontend-ui/14-VERIFICATION.md

# Current state (from 14-01-SUMMARY.md):
# - Language field exists in singleForm and bulkForm state with default 'vietnamese'
# - Language radio buttons present in UI for both single and bulk forms
# - Form resets language to 'vietnamese' when modal opens (lines 74-90, 92-108)

# Gap (from 14-VERIFICATION.md):
# - LANG-06 requirement not implemented: no localStorage persistence
# - Language selection resets to default when modal opens
# - Missing: localStorage.getItem() on component initialization
# - Missing: localStorage.setItem() when language selection changes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add localStorage persistence for language selection</name>
  <files>frontend/src/components/Projects/ProjectDetail.jsx</files>
  <action>
    Modify the useEffect hook that initializes forms when showCreateModal is true (lines 74-108). Instead of hardcoding language: 'vietnamese', read from localStorage using localStorage.getItem('languagePreference'). If no saved preference exists, default to 'vietnamese'. This ensures the user's last language selection is restored when opening the modal.

    Add a new useEffect hook that watches for changes to singleForm.language and bulkForm.language. When the language value changes, save it to localStorage using localStorage.setItem('languagePreference', newValue). This ensures the preference is persisted whenever the user changes the language selection.

    The localStorage key should be 'languagePreference' and the stored values should be 'vietnamese' or 'english' (matching the form state values).

    Reference existing code: The form initialization is in the useEffect at lines 74-108. The language field is currently hardcoded to 'vietnamese' at lines 39, 53, 89, and 103.

    Gap reason: LANG-06 requirement requires language selection to persist when user navigates away and returns. Currently, language resets to 'vietnamese' on every modal open.
  </action>
  <verify>
    File contains localStorage.getItem('languagePreference') in form initialization useEffect, and contains localStorage.setItem('languagePreference', ...) in a useEffect that depends on language state
  </verify>
  <done>
    Language selection is loaded from localStorage when modal opens, and saved to localStorage when user changes language selection. User's language preference persists across page navigation and modal open/close cycles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→localStorage | User's language preference stored in browser localStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering | localStorage language preference | accept | localStorage is client-side only, user can modify it. No security impact - preference is for UX only, not authorization or data access. Backend validates language on submission (LANG-08 from Phase 12). |
| T-14-08 | Information disclosure | localStorage reveals language preference | accept | Language preference is not sensitive information. No PII or security data stored. |
</threat_model>

<verification>
- Language selection is loaded from localStorage when Create Post modal opens
- Language selection is saved to localStorage when user changes language
- Language preference persists when user navigates away and returns to the form
- If no saved preference exists, defaults to 'vietnamese'
- localStorage key is 'languagePreference' with values 'vietnamese' or 'english'
</verification>

<success_criteria>
- Users' language selection persists across page navigation
- Users' language selection persists when modal is closed and reopened
- Language preference is stored in localStorage
- Default behavior (vietnamese) works when no preference is saved
- No errors when localStorage is disabled or unavailable
</success_criteria>

<output>
After completion, create `.planning/phases/14-frontend-ui/14-04-SUMMARY.md`
</output>

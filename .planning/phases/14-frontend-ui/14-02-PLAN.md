---
phase: 14-frontend-ui
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified: [frontend/src/components/Projects/ProjectDetail.jsx]
autonomous: true
requirements: [LANG-02]
user_setup: []

must_haves:
  truths:
    - "Language badge is displayed in post list table"
    - "Language badge uses color coding (green for Vietnamese, blue for English)"
    - "Language badge shows language name"
    - "Language badge is visible for all posts in the list"
  artifacts:
    - path: "frontend/src/components/Projects/ProjectDetail.jsx"
      provides: "Language badge component and display in post list table"
      min_lines: 980
  key_links:
    - from: "frontend/src/components/Projects/ProjectDetail.jsx"
      to: "post.language"
      via: "LanguageBadge component prop"
      pattern: "LanguageBadge.*language"
    - from: "LanguageBadge component"
      to: "CSS classes"
      via: "Conditional class based on language value"
      pattern: "badge-vietnamese|badge-english"
---

<objective>
Add language badge display to the post list table in ProjectDetail component.

Purpose: Allow users to identify the language of each post at a glance with color-coded badges.
Output: Updated post list table with language column and LanguageBadge component.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-backend-foundation/12-02-SUMMARY.md
@.planning/phases/12-backend-foundation/12-03-SUMMARY.md

# Backend API returns language field in post responses (from Phase 12)
# PostResponse model includes language field
# GET /api/posts/by-project/{project_id} returns language for each post
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LanguageBadge component</name>
  <files>frontend/src/components/Projects/ProjectDetail.jsx</files>
  <action>Create a new LanguageBadge component function at the bottom of ProjectDetail.jsx (similar to existing BoolBadge and JobStatusBadge components). The component should accept a language prop ("vietnamese" or "english") and render a badge with color coding: green background for Vietnamese, blue background for English. Use CSS classes like "language-badge badge-vietnamese" and "language-badge badge-english" for styling. Display the language name: "Tiếng Việt" for Vietnamese, "English" for English. Add PropTypes validation for the language prop.</action>
  <verify>LanguageBadge component exists with language prop, renders badge with color coding and language name</verify>
  <done>LanguageBadge component created with color-coded display for Vietnamese and English</done>
</task>

<task type="auto">
  <name>Task 2: Add language column to post list table</name>
  <files>frontend/src/components/Projects/Projects/ProjectDetail.jsx</files>
  <action>Add a new "Language" column to the post list table header (after "Title / Topic" column, before "Research" column). In the table body, render the LanguageBadge component for each post, passing the post.language value as the prop. Handle missing language field gracefully by defaulting to "English" (using post.language || 'english'). Follow the existing table structure pattern (lines 500-553).</action>
  <verify>Post list table has Language column with LanguageBadge component displaying language for each post</verify>
  <done>Language column added to post list table with color-coded language badges</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→client | Language field from API response crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Tampering | Language display from API | accept | Backend controls language field, client only displays what API returns |
| T-14-04 | Information disclosure | Language badge reveals content language | accept | Language is not sensitive information, display is intentional feature |
</threat_model>

<verification>
- Language column appears in post list table
- Language badge displays correct color (green for Vietnamese, blue for English)
- Language badge displays correct language name
- Posts without language field default to English badge
- Badge styling matches design system
</verification>

<success_criteria>
- Language badge is visible in post list table
- Color coding is correct (green for Vietnamese, blue for English)
- Language name is displayed correctly
- Missing language field is handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/14-frontend-ui/14-02-SUMMARY.md`
</output>

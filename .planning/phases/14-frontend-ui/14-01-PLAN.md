---
phase: 14-frontend-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend/src/components/Projects/ProjectDetail.jsx]
autonomous: true
requirements: [LANG-01]
user_setup: []

must_haves:
  truths:
    - "User can select Vietnamese or English language when creating a post"
    - "Vietnamese is selected by default"
    - "Language selection persists when form is submitted with errors"
    - "Language selection is included in API request when creating post"
  artifacts:
    - path: "frontend/src/components/Projects/ProjectDetail.jsx"
      provides: "Language selection radio buttons in Create Post form"
      min_lines: 970
  key_links:
    - from: "frontend/src/components/Projects/ProjectDetail.jsx"
      to: "POST /api/posts"
      via: "formData.append('language', singleForm.language)"
      pattern: "formData.append.*language"
    - from: "frontend/src/components/Projects/ProjectDetail.jsx"
      to: "POST /api/posts/bulk"
      via: "language field in bulkForm object"
      pattern: "language.*bulkForm"
---

<objective>
Add language selection radio buttons to the Create Post form for both single and bulk post creation modes.

Purpose: Allow users to choose between Vietnamese and English language for AI-generated content, with Vietnamese as the default selection.
Output: Updated Create Post form with language radio buttons and form state management.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-backend-foundation/12-02-SUMMARY.md
@.planning/phases/12-backend-foundation/12-03-SUMMARY.md

# Backend API already supports language field (from Phase 12)
# PostCreate and BulkPostCreate models accept language field
# API endpoints validate and store language in MongoDB
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add language field to form state and radio buttons UI</name>
  <files>frontend/src/components/Projects/ProjectDetail.jsx</files>
  <action>Add language field to singleForm and bulkForm state objects with default value "vietnamese". Add radio button group for language selection after "Topic" field and before "Additional Requests" field in both single and bulk post forms. Radio buttons should have two options: "Tiếng Việt" (Vietnamese) and "English", with Vietnamese selected by default. Use the same radio button pattern as the existing thumbnail_source field (lines 736-768 for single form, lines 838-871 for bulk form).</action>
  <verify>File contains language field in singleForm and bulkForm state objects, and radio button groups for language selection in both forms</verify>
  <done>Language field added to form state with default "vietnamese", radio buttons displayed in both single and bulk post forms</done>
</task>

<task type="auto">
  <name>Task 2: Update form submission to include language in API calls</name>
  <files>frontend/src/components/Projects/ProjectDetail.jsx</files>
  <action>Update handleCreateSingle() function to append language field to FormData before calling createPost() API. Update handleCreateBulk() function to include language field in the request object before calling createBulkPosts() API. Follow the existing pattern for other form fields (e.g., ai_provider_id, model_name, thumbnail_source).</action>
  <verify>handleCreateSingle() includes formData.append('language', singleForm.language), handleCreateBulk() includes language in request object</verify>
  <done>Language field included in API requests for both single and bulk post creation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | User-selected language crosses here via HTTP POST |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | Language parameter in form submission | accept | Backend validates language is "vietnamese" or "english" (Phase 12 LANG-08), no additional client-side validation needed |
| T-14-02 | Spoofing | Form submission with manipulated language | accept | Backend Pydantic validation restricts to valid values, client-side manipulation cannot bypass server validation |
</threat_model>

<verification>
- Language radio buttons appear in both single and bulk post forms
- Vietnamese is selected by default
- Language selection persists when form is submitted with errors
- Language field is included in API requests
- Form submission succeeds with language field
</verification>

<success_criteria>
- Users can select Vietnamese or English language when creating posts
- Vietnamese is the default selection
- Language selection is included in API requests
- Form validation and submission work correctly with language field
</success_criteria>

<output>
After completion, create `.planning/phases/14-frontend-ui/14-01-SUMMARY.md`
</output>

---
phase: 14-frontend-ui
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [frontend/package.json, frontend/vite.config.js, frontend/src/components/Projects/ProjectDetail.test.jsx, frontend/src/components/Posts/PostView.test.jsx]
autonomous: true
gap_closure: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Frontend tests pass"
  artifacts:
    - path: "frontend/package.json"
      provides: "Testing framework dependencies"
      contains: "vitest|@testing-library"
    - path: "frontend/vite.config.js"
      provides: "Vitest configuration"
      contains: "test:|vitest"
    - path: "frontend/src/components/Projects/ProjectDetail.test.jsx"
      provides: "Unit tests for ProjectDetail component"
      min_lines: 50
    - path: "frontend/src/components/Posts/PostView.test.jsx"
      provides: "Unit tests for PostView component"
      min_lines: 50
  key_links:
    - from: "npm test"
      to: "Vitest test runner"
      via: "package.json test script"
      pattern: "\"test\":.*vitest"
    - from: "ProjectDetail.test.jsx"
      to: "ProjectDetail.jsx"
      via: "import statement"
      pattern: "import.*ProjectDetail"
    - from: "PostView.test.jsx"
      to: "PostView.jsx"
      via: "import statement"
      pattern: "import.*PostView"
---

<objective>
Set up frontend testing infrastructure and create unit tests for language selection UI and language badge components.

Purpose: Ensure code quality and prevent regressions by adding automated tests for the language selection and display features implemented in Phase 14.
Output: Testing framework configured with Vitest and React Testing Library, test files for ProjectDetail and PostView components.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/14-frontend-ui/14-01-SUMMARY.md
@.planning/phases/14-frontend-ui/14-02-SUMMARY.md
@.planning/phases/14-frontend-ui/14-03-SUMMARY.md
@.planning/phases/14-frontend-ui/14-VERIFICATION.md

# Current state (from package.json):
# - No testing dependencies installed
# - No test scripts configured
# - No test files exist in frontend/src

# Gap (from 14-VERIFICATION.md):
# - No frontend tests mentioned in summaries
# - Success criteria requires unit tests for React components
# - Missing: test files for ProjectDetail.jsx and PostView.jsx
# - Missing: unit tests for language selection UI
# - Missing: unit tests for LanguageBadge component

# Testing framework choice:
# - Vitest: Fast, native ESM support, works with Vite (already in use)
# - React Testing Library: Standard for React component testing, focuses on user behavior
# - jsdom: DOM environment for testing (required by React Testing Library)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install testing dependencies and configure Vitest</name>
  <files>frontend/package.json, frontend/vite.config.js</files>
  <action>
    Install testing dependencies by running:
    cd frontend && npm install --save-dev vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom happy-dom

    Update frontend/package.json to add test scripts:
    - Add "test": "vitest" to scripts section
    - Add "test:ui": "vitest --ui" to scripts section (optional, for visual test runner)
    - Add "test:coverage": "vitest --coverage" to scripts section (optional, for coverage reports)

    Update frontend/vite.config.js to configure Vitest:
    - Add test configuration object to the Vite config
    - Set environment to 'jsdom' or 'happy-dom' for DOM testing
    - Set globals to true for describe, test, expect without imports
    - Add setupFiles: ['./src/test/setup.js'] (create this file for global test configuration)
    - Configure coverage if desired (coverage.provider: 'v8', coverage.all: true)

    Create frontend/src/test/setup.js with:
    - Import @testing-library/jest-dom to extend Jest matchers
    - Any global test configuration needed

    Reference existing code: frontend/vite.config.js already has Vite configuration for React plugin. Add test configuration alongside existing config.

    Gap reason: No testing infrastructure exists. Need to set up Vitest and React Testing Library before creating test files.
  </action>
  <verify>
    package.json contains vitest, @testing-library/react, @testing-library/jest-dom in devDependencies, and "test" script in scripts. vite.config.js contains test configuration with environment set to jsdom or happy-dom.
  </verify>
  <done>
    Testing framework installed and configured. Can run tests with `npm test`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for ProjectDetail language selection UI</name>
  <files>frontend/src/components/Projects/ProjectDetail.test.jsx</files>
  <action>
    Create frontend/src/components/Projects/ProjectDetail.test.jsx with unit tests for language selection functionality.

    Test cases to include:
    1. Language field is initialized with 'vietnamese' by default in singleForm state
    2. Language field is initialized with 'vietnamese' by default in bulkForm state
    3. Language radio buttons are rendered in single post form
    4. Language radio buttons are rendered in bulk post form
    5. Vietnamese radio button is selected by default
    6. Clicking English radio button updates singleForm.language to 'english'
    7. Clicking Vietnamese radio button updates singleForm.language to 'vietnamese'
    8. Clicking English radio button updates bulkForm.language to 'english'
    9. Clicking Vietnamese radio button updates bulkForm.language to 'vietnamese'
    10. Language field is included in API request when creating single post (mock API call)
    11. Language field is included in API request when creating bulk posts (mock API call)

    Use React Testing Library patterns:
    - render() to render the component
    - screen.getByLabelText() or screen.getByRole() to find radio buttons
    - fireEvent.click() or userEvent.click() to simulate user interactions
    - expect().toBeInTheDocument() to verify elements are rendered
    - expect().toHaveValue() or expect().toBeChecked() to verify radio button state
    - Mock API calls using vi.fn() from Vitest

    Reference existing code: ProjectDetail.jsx has language field at lines 39, 53, 89, 103. Radio buttons are in the form (lines 818-843 for single, 948-973 for bulk). LanguageBadge component is at lines 1028-1040.

    Gap reason: No test file exists for ProjectDetail.jsx. Need to verify language selection UI works correctly.
  </action>
  <verify>
    ProjectDetail.test.jsx file exists with at least 11 test cases covering language selection functionality. Tests can be run with `npm test` and pass.
  </verify>
  <done>
    Unit tests for ProjectDetail language selection UI created and passing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for PostView language badge display</name>
  <files>frontend/src/components/Posts/PostView.test.jsx</files>
  <action>
    Create frontend/src/components/Posts/PostView.test.jsx with unit tests for language badge display functionality.

    Test cases to include:
    1. Language badge is rendered in post detail view when post has language field
    2. Language badge displays "Tiếng Việt" when post.language is 'vietnamese'
    3. Language badge displays "English" when post.language is 'english'
    4. Language badge uses green color (var(--success)) for Vietnamese
    5. Language badge uses blue color (var(--primary)) for English
    6. Language badge is positioned in page header area
    7. Language badge defaults to "English" when post.language is missing or null
    8. Language badge handles undefined language gracefully

    Use React Testing Library patterns:
    - render() to render the component with mock post data
    - screen.getByText() or screen.getByRole() to find language badge
    - expect().toHaveTextContent() to verify language name
    - expect().toHaveStyle() to verify color coding (check inline styles)
    - Mock API calls using vi.fn() from Vitest

    Reference existing code: PostView.jsx has LanguageBadge component at lines 505-517, used in header at line 186. LanguageBadge uses inline styles for color coding (lines 506-509).

    Gap reason: No test file exists for PostView.jsx. Need to verify language badge display works correctly.
  </action>
  <verify>
    PostView.test.jsx file exists with at least 8 test cases covering language badge display functionality. Tests can be run with `npm test` and pass.
  </verify>
  <done>
    Unit tests for PostView language badge display created and passing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→component | Test code exercises component behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | Tampering | Test mocks | accept | Tests use mocks to isolate components. Mocks are controlled by test code, not external input. No security impact. |
| T-14-10 | Information disclosure | Test code reveals implementation details | accept | Test code is not deployed to production. Implementation details in tests are not exposed to users. |
</threat_model>

<verification>
- Testing dependencies installed (vitest, @testing-library/react, @testing-library/jest-dom, jsdom or happy-dom)
- Test scripts added to package.json (npm test)
- Vitest configured in vite.config.js
- ProjectDetail.test.jsx exists with language selection tests
- PostView.test.jsx exists with language badge tests
- All tests pass when running `npm test`
- Test coverage for language selection and badge display features
</verification>

<success_criteria>
- Frontend testing infrastructure is set up and working
- Unit tests exist for ProjectDetail language selection UI
- Unit tests exist for PostView language badge display
- All tests pass
- Tests can be run with `npm test`
- Test files follow React Testing Library best practices
</success_criteria>

<output>
After completion, create `.planning/phases/14-frontend-ui/14-05-SUMMARY.md`
</output>

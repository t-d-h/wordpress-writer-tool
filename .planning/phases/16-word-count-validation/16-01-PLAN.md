---
phase: 16
plan: 01
type: execute
wave: 1
needs:
  - 15-VALIDATION-RESULTS-DISPLAY
  - 15-HTML-CLEANING
files_modified:
  - .planning/phases/16-WORD-COUNT-VALIDATION/16-01-PLAN.md
  - src/validation/word_count.py
  - src/validation/__init__.py
autonomous: false
requirements:
  - D-16-VALIDATION-ARCHITECTURE
  - D-16-WORD-COUNT-VALIDATION
  - D-16-VALIDATION-RESULTS-DISPLAY
user_setup: []
must_haves:
  truths:
    - "Word count validation service exists and is testable"
    - "Word count can be calculated from cleaned HTML content"
    - "Validation results include word count metrics"
  artifacts:
    - path: "src/validation/word_count.py"
      provides: "Word count calculation service"
      min_lines: 30
    - path: "src/validation/__init__.py"
      provides: "Word count module exports"
      min_lines: 5
    - path: ".planning/phases/16-WORD-COUNT-VALIDATION/16-01-PLAN.md"
      provides: "Phase 16 plan documentation"
    - path: ".planning/phases/16-WORD-COUNT-VALIDATION/16-01-PLAN.pre-bounce.md"
      provides: "Backup of pre-bounce plan (if bounce occurs)"
  key_links:
    - from: "src/validation/word_count.py"
      to: "src/validation/__init__.py"
      via: "imports"
      pattern: "from.*word_count.*import"
    - from: "src/validation/word_count.py"
      to: "src/content/cleaner.py"
      via: "uses"
      pattern: "from.*cleaner.*import"
    - from: ".planning/phases/16-WORD-COUNT-VALIDATION/16-01-PLAN.md"
      to: "src/validation/word_count.py"
      via: "implements"
      pattern: "WordCountValidator"

---

<objective>
Implement word count validation service for content quality improvements phase

Purpose: Provide word count validation capability for content quality metrics
Output: Word count validation service with tests and integration
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/references/ui-brand.md
@$HOME/.config/kilo/get-shit-done/references/agent-contracts.md
</execution_context>

<context>
**Phase Context:** Word Count Validation - part of v1.3 Content Quality Improvements
**Phase Goal:** Implement validation that counts words in cleaned HTML content and provides metrics for quality assessment
**Dependencies:** HTML cleaning service from Phase 15, validation results display
**Constraints:** Use existing cleaner service, follow project validation patterns

**Key Interfaces:**
- `WordCountService` - calculates word count from cleaned HTML
- `WordCountValidator` - validates content meets minimum/maximum word requirements
- `ValidationResult` - includes word count metrics in validation output

**Project Patterns:**
- Validation services follow `{ServiceName}Service` naming convention
- Tests use `{ServiceName}ServiceTest` pattern  
- Services expose `validate()` method returning `ValidationResult`
- Integration uses existing validation framework from Phase 15
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Word Count Service Interface</name>
  <files>
    src/validation/word_count.py,
    src/validation/__init__.py
  </files>
  <action>
    Create `word_count.py` with `WordCountService` class implementing:
    - `count_words(html_content: str) -> int` method to count words in cleaned HTML
    - Strips HTML tags before counting
    - Handles empty/None content gracefully
    
    Export in `__init__.py`:
    - `from .word_count import WordCountService`
    - `__all__ = ['WordCountService']`
  </action>
  <verify>
    <automated>
      python -c "from src.validation.word_count import WordCountService; svc = WordCountService(); assert callable(getattr(svc, 'count_words', None))"
    </automated>
  </verify>
  <done>
    Interface defined with `WordCountService` class and `count_words` method
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Word Count Logic</name>
  <files>
    src/validation/word_count.py
  </files>
  <action>
    Implement word counting logic:
    - Use regex `r'\w+'` to extract words from HTML text content
    - Strip HTML tags using regex or BeautifulSoup if available
    - Count non-empty word tokens
    - Return integer count
    
    Add unit tests in `src/validation/test_word_count.py`:
    - Test empty string returns 0
    - Test simple text returns correct count
    - Test HTML with tags returns text word count
    - Test handles None/whitespace gracefully
  </action>
  <verify>
    <automated>
      python -m pytest src/validation/test_word_count.py -v
    </automated>
  </verify>
  <done>
    Word counting implemented and tests passing
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate with Validation Framework</name>
  <files>
    src/validation/__init__.py,
    .planning/phases/16-WORD-COUNT-VALIDATION/16-01-PLAN.md
  </files>
  <action>
    - Add `WordCountValidator` class that uses `WordCountService`
    - Implement `validate(html_content: str) -> ValidationResult` method
    - Check word count against configurable min/max thresholds
    - Return validation result with word count metrics
    - Update plan documentation with implementation details and test results
  </action>
  <verify>
    <automated>
      python -c "from src.validation import WordCountValidator; v = WordCountValidator(min_words=10, max_words=1000); assert hasattr(v, 'validate')"
    </automated>
  </verify>
  <done>
    Word count validator integrated with validation framework
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {client→API} | User input HTML content for word counting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-16-01 | Tampering | word_count.py | mitigate | Input validation, HTML sanitization |
| T-16-02 | Information Disclosure | validation results | accept | No PII in word count data |
</threat_model>

<verification>
## Phase Verification Steps

1. **Unit Tests**: Run `pytest src/validation/test_word_count.py -v` - all tests pass
2. **Integration Test**: Verify `WordCountValidator` works with sample HTML content
3. **Export Check**: Confirm `WordCountService` is exported from `src/validation/__init__.py`
4. **Documentation**: Verify plan file documents implementation and test results

## Success Criteria

- [ ] `WordCountService.count_words()` correctly counts words from HTML
- [ ] `WordCountValidator.validate()` returns proper validation results
- [ ] All unit tests pass
- [ ] Service integrates with existing validation framework
- [ ] Plan documentation updated with implementation details
</verification>

<success_criteria>
Measurable completion:
1. `src/validation/word_count.py` exists with `WordCountService` class
2. `src/validation/__init__.py` exports `WordCountService`
3. Test file `src/validation/test_word_count.py` exists and passes
4. `WordCountValidator` class integrates with validation framework
5. `.planning/phases/16-WORD-COUNT-VALIDATION/16-01-PLAN.md` documents implementation
6. All automated verification checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/16-WORD-COUNT-VALIDATION/16-01-PLAN-SUMMARY.md` with:
- Implementation summary
- Test results
- Integration status
- Next phase recommendations
</output>
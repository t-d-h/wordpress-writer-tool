---
phase: 06-frontend-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/client.js
  - frontend/src/components/AllPosts.jsx
autonomous: true
requirements:
  - FRONTEND-02
  - FRONTEND-03
user_setup: []

must_haves:
  truths:
    - "User can search posts by title using search input field"
    - "User can sort posts by date, title, or status using sort dropdown"
    - "Search and sort parameters are passed to backend API"
    - "Page resets to 1 when search or sort changes"
  artifacts:
    - path: "frontend/src/api/client.js"
      provides: "getSitePosts function with search and sort parameters"
      exports: ["getSitePosts"]
    - path: "frontend/src/components/AllPosts.jsx"
      provides: "Search and sort state management"
      contains: "searchQuery, sortBy, sortOrder state variables"
  key_links:
    - from: "frontend/src/components/AllPosts.jsx"
      to: "frontend/src/api/client.js"
      via: "getSitePosts function call"
      pattern: "getSitePosts.*search.*orderby"
    - from: "frontend/src/components/AllPosts.jsx"
      to: "backend/app/routers/wp_sites.py"
      via: "API request with search and sort parameters"
      pattern: "search.*orderby.*order"
---

<objective>
Update API client and add search/sort state management to AllPosts component

Purpose: Enable frontend to pass search and sort parameters to backend API for filtering and ordering posts
Output: Updated getSitePosts function accepting search/sort parameters, AllPosts component with search/sort state
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-ui/06-UI-SPEC.md

# Backend API contract (from wp_sites.py lines 102-191)
```python
@router.get("/{site_id}/posts")
async def get_site_posts(
    site_id: str,
    per_page: int = 100,
    page: int = 1,
    status: str = None,
    search: str = None,
    orderby: str = "date",
    order: str = "desc",
):
```

Allowed orderby values: ["date", "title", "modified", "relevance"]
Allowed order values: ["asc", "desc"]

# Frontend patterns (from Phase 03 summary)
- Pattern: Backend pagination with Query parameters
- Pattern: Backend filtering: Query parameters map to MongoDB query filters
- Pattern: Backend sorting: sort_by parameter maps to MongoDB sort field and direction
- Pattern: Pagination reset on filter/sort/search changes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update getSitePosts API client to accept search and sort parameters</name>
  <files>frontend/src/api/client.js</files>
  <action>Update the getSitePosts function (lines 31-35) to accept search, orderby, and order parameters. Add these to the params object if provided. Keep existing per_page, page, and status parameters. Default orderby to "date" and order to "desc" to match backend defaults.</action>
  <verify>File exists with updated getSitePosts function signature including search, orderby, and order parameters</verify>
  <done>getSitePosts function accepts search, orderby, and order parameters and passes them to backend API</done>
</task>

<task type="auto">
  <name>Task 2: Add search and sort state to AllPosts component</name>
  <files>frontend/src/components/AllPosts.jsx</files>
  <action>Add three new state variables after line 13: searchQuery (default empty string), sortBy (default "date"), and sortOrder (default "desc"). Update the loadPosts function to pass these parameters to getSitePosts call. Update the useEffect dependency array on line 24 to include searchQuery, sortBy, and sortOrder. Add event handlers for search input (setSearchQuery, setPage(1)) and sort dropdown (setSortBy, setPage(1)).</action>
  <verify>Component has searchQuery, sortBy, and sortOrder state variables; loadPosts passes these to getSitePosts; useEffect includes new dependencies</verify>
  <done>AllPosts component manages search and sort state, passes parameters to API, resets page on changes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | User input (search query) crosses here |
| client→API | Sort parameters cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | I | getSitePosts search parameter | mitigate | Backend already validates search parameter in wp_service.py (line 118-122) - allowed_orderby check prevents injection |
| T-06-02 | I | getSitePosts orderby parameter | mitigate | Backend already validates orderby parameter (line 117-122) - only allows ["date", "title", "modified", "relevance"] |
| T-06-03 | I | getSitePosts order parameter | mitigate | Backend already validates order parameter (line 124-127) - only allows ["asc", "desc"] |
| T-06-04 | X | search query in URL | accept | No sensitive data in search queries, low-value target for XSS |
</threat_model>

<verification>
- getSitePosts function accepts search, orderby, and order parameters
- AllPosts component has searchQuery, sortBy, and sortOrder state variables
- loadPosts function passes search and sort parameters to getSitePosts
- useEffect includes searchQuery, sortBy, and sortOrder in dependency array
- Page resets to 1 when search or sort changes
- Backend validation already in place for orderby and order parameters
</verification>

<success_criteria>
- getSitePosts function signature includes search, orderby, and order parameters
- AllPosts component has three new state variables for search and sort
- Search and sort parameters are passed to backend API
- Page resets to 1 when search or sort changes
- No backend changes required (validation already in place)
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-ui/06-01-SUMMARY.md`
</output>

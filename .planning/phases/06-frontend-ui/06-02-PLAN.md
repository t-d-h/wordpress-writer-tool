---
phase: 06-frontend-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - frontend/src/components/AllPosts.jsx
  - frontend/src/index.css
autonomous: true
requirements:
  - FRONTEND-01
  - FRONTEND-02
  - FRONTEND-03
  - FRONTEND-06
  - FRONTEND-07
user_setup: []

must_haves:
  truths:
    - "User sees search input field in toolbar"
    - "User sees sort dropdown in toolbar"
    - "Search input filters posts by title"
    - "Sort dropdown orders posts by selected field"
    - "Loading state displays when search/sort changes"
    - "Empty state displays when no posts match search criteria"
  artifacts:
    - path: "frontend/src/components/AllPosts.jsx"
      provides: "Search and sort UI controls"
      contains: "search input, sort dropdown, event handlers"
    - path: "frontend/src/index.css"
      provides: "Search input styling"
      contains: ".toolbar-search, .toolbar-sort classes"
  key_links:
    - from: "search input onChange"
      to: "setSearchQuery state"
      via: "event handler"
      pattern: "setSearchQuery.*setPage.*1"
    - from: "sort dropdown onChange"
      to: "setSortBy state"
      via: "event handler"
      pattern: "setSortBy.*setPage.*1"
    - from: "search/sort state"
      to: "loadPosts function"
      via: "useEffect dependency"
      pattern: "useEffect.*searchQuery.*sortBy"
---

<objective>
Add search input and sort dropdown UI controls to AllPosts toolbar

Purpose: Provide user interface for searching posts by title and sorting by date, title, or status
Output: Search input field and sort dropdown in toolbar, wired to state management
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-frontend-ui/06-UI-SPEC.md
@.planning/phases/06-frontend-ui/06-01-SUMMARY.md

# Existing toolbar pattern (from AllPosts.jsx lines 88-132)
```jsx
<div className="toolbar">
  <div className="toolbar-group">
    <label className="form-label">Site:</label>
    <select className="form-select">...</select>
  </div>
  <div className="toolbar-group">
    <label className="form-label">Status:</label>
    <select className="form-select">...</select>
  </div>
  <button className="btn btn-secondary">Refresh</button>
</div>
```

# Existing form styles (from index.css lines 445-487)
- .form-input, .form-select - Base form control styles
- .form-input:focus, .form-select:focus - Focus states
- .form-select - Custom dropdown arrow

# Toolbar styles (from index.css lines 1064-1075)
- .toolbar - Flex container with gap
- .toolbar-actions - Action buttons container

# UI spec requirements (from 06-UI-SPEC.md)
- Primary CTA: "Search Posts"
- Empty state heading: "No Posts Found"
- Empty state body: "No posts match your search criteria. Try adjusting your filters or search terms."
- Spacing: xs (4px), sm (8px), md (16px), lg (24px)
- Typography: Body (14px), Label (12px)
- Color: Accent (#6c5ce7) for primary buttons, active states
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add search input field to toolbar</name>
  <files>frontend/src/components/AllPosts.jsx</files>
  <action>Add a new toolbar-group div after the status filter (after line 123) containing a search input field. Use .form-input class for styling. Add a label "Search:" with .form-label class. The input should have placeholder text "Search by title...". Add onChange handler that sets searchQuery state and resets page to 1. Add value={searchQuery} to bind to state. Add a search icon button next to the input using HiOutlineMagnifyingGlass from react-icons/hi2.</action>
  <verify>Search input field exists in toolbar with proper styling, onChange handler, and value binding</verify>
  <done>Search input field added to toolbar, filters posts by title, resets page on change</done>
</task>

<task type="auto">
  <name>Task 2: Add sort dropdown to toolbar</name>
  <files>frontend/src/components/AllPosts.jsx</files>
  <action>Add a new toolbar-group div after the search input containing a sort dropdown. Use .form-select class for styling. Add a label "Sort:" with .form-label class. The select should have options: "Date (Newest)" (value="date-desc"), "Date (Oldest)" (value="date-asc"), "Title (A-Z)" (value="title-asc"), "Title (Z-A)" (value="title-desc"), "Status" (value="status-asc"). Add onChange handler that parses the value (split by "-") to set sortBy and sortOrder state, and resets page to 1. Add value={`${sortBy}-${sortOrder}`} to bind to state.</action>
  <verify>Sort dropdown exists in toolbar with proper styling, onChange handler, and value binding</verify>
  <done>Sort dropdown added to toolbar, orders posts by selected field, resets page on change</done>
</task>

<task type="auto">
  <name>Task 3: Add CSS styling for search and sort controls</name>
  <files>frontend/src/index.css</files>
  <action>Add CSS classes for toolbar search and sort controls. Add .toolbar-search and .toolbar-sort classes with flex layout, gap of 8px, and proper alignment. Add .toolbar-search-input class with width of 200px and min-width of 150px. Add .toolbar-sort-select class with width of 180px and min-width of 140px. Ensure proper spacing and alignment with existing toolbar elements. Use existing design tokens (--spacing-md, --radius-sm, --border-color).</action>
  <verify>CSS classes exist for toolbar search and sort controls with proper styling</verify>
  <done>Search and sort controls styled consistently with existing toolbar elements</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input→state | Search query enters application state |
| user input→state | Sort selection enters application state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | X | search input value | accept | No sensitive data, React state binding prevents XSS |
| T-06-06 | I | sort dropdown values | mitigate | Hardcoded option values prevent injection |
| T-06-07 | T | search input placeholder | accept | Static text, no user-generated content |
</threat_model>

<verification>
- Search input field exists in toolbar with proper styling
- Sort dropdown exists in toolbar with proper styling
- Search input onChange handler sets searchQuery and resets page
- Sort dropdown onChange handler sets sortBy/sortOrder and resets page
- Search input value is bound to searchQuery state
- Sort dropdown value is bound to sortBy-sortOrder state
- CSS classes provide proper spacing and alignment
- Loading state displays when search/sort changes (existing fetchingPosts state)
- Empty state displays when no posts match search criteria (existing empty state)
</verification>

<success_criteria>
- Search input field visible in toolbar with "Search by title..." placeholder
- Sort dropdown visible in toolbar with 5 sort options
- Search filters posts by title in real-time
- Sort orders posts by selected field and direction
- Page resets to 1 when search or sort changes
- Controls styled consistently with existing toolbar elements
- Loading state displays during API calls
- Empty state displays when no results found
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-ui/06-02-SUMMARY.md`
</output>

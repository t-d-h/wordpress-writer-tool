---
phase: 15-html-cleaning-foundation
plan: '02'
type: execute
wave: 2
depends_on:
  - 15-01-PLAN.md
files_modified:
  - backend/tests/test_ai_service.py
autonomous: true
requirements:
  - HTML-01
  - HTML-02
  - HTML-03

must_haves:
  truths:
    - "Tests verify clean_html() strips backticks and markdown code blocks (HTML-01)"
    - "Tests verify clean_html() removes disallowed tags while preserving allowed ones (HTML-03)"
    - "Tests verify clean_html() produces WordPress-ready output with no markdown artifacts (HTML-02)"
  artifacts:
    - path: "backend/tests/test_ai_service.py"
      provides: "Unit tests for clean_html function"
      contains: "test_clean_html"
      contains2: "def test_clean_html_strips_backticks"
      contains3: "def test_clean_html_removes_disallowed_tags"
    - path: "backend/tests/test_ai_service.py"
      provides: "pytest configuration exists"
      contains: "pytest"
</must_haves>
---

<objective>
Create unit tests for the clean_html() function covering all three phase requirements: HTML-01 (clean output), HTML-02 (no markdown artifacts), HTML-03 (HTML sanitization).
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-html-cleaning-foundation/15-01-PLAN-SUMMARY.md
@backend/app/services/ai_service.py
@backend/tests/test_ai_service.py

## Test Coverage Requirements

From RESEARCH.md validation architecture, each requirement must have test coverage:

| Req ID | Behavior | Test |
|--------|----------|------|
| HTML-01 | Strips backticks from content | test_clean_html_strips_backticks |
| HTML-01 | Strips markdown code blocks | test_clean_html_strips_code_blocks |
| HTML-02 | No markdown artifacts in output | test_clean_html_no_markdown |
| HTML-03 | Allowed tags preserved | test_clean_html_preserves_allowed_tags |
| HTML-03 | Disallowed tags removed | test_clean_html_removes_disallowed_tags |

## Test Cases from RESEARCH.md

```python
TEST_CASES = [
    # (input, expected_contains, expected_not_contains)
    ("```html\n<p>Test</p>\n```", "<p>Test</p>", "```"),
    ("<script>alert('xss')</script><p>Safe</p>", "<p>Safe</p>", "<script>"),
    ("<p style='color:red'>Styled</p>", "<p>Styled</p>", "style="),
    ("<h1>Title</h1><div>Bad</div>", "<h1>Title</h1>", "<div>"),
]
```

## Test File Location

The tests should be added to: `backend/tests/test_ai_service.py`
If this file doesn't exist, create it with proper pytest structure.

If backend/tests/ directory doesn't exist, create it first.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create/verify test file structure</name>
  <files>backend/tests/test_ai_service.py</files>
  <action>
    If backend/tests/ directory doesn't exist, create it.
    If backend/tests/test_ai_service.py doesn't exist, create it with:
    - pytest import
    - import of clean_html from app.services.ai_service
    - Basic test class or standalone test functions
    
    If the file already exists, add clean_html tests to it (don't overwrite existing tests).
  </action>
  <verify>
    python -c "import sys; sys.path.insert(0, 'backend'); from tests.test_ai_service import *" 2>&1 || echo "Test file may need setup"
  </verify>
  <done>backend/tests/test_ai_service.py exists and imports clean_html</done>
</task>

<task type="auto">
  <name>Task 2: Add test_clean_html_strips_backticks</name>
  <files>backend/tests/test_ai_service.py</files>
  <action>
    Add test function: test_clean_html_strips_backticks
    
    Test cases:
    - Input: '`<p>Hello</p>`' → should not contain backtick
    - Input: '``code``' → should not contain backtick
    - Input with backticks around HTML: '`<h1>Title</h1>`' → should be clean h1
    
    Verify: '`' not in result
  </action>
  <verify>
    pytest backend/tests/test_ai_service.py::test_clean_html_strips_backticks -x -v 2>&1 | tail -5
  </verify>
  <done>test_clean_html_strips_backticks exists and passes</done>
</task>

<task type="auto">
  <name>Task 3: Add test_clean_html_strips_code_blocks</name>
  <files>backend/tests/test_ai_service.py</files>
  <action>
    Add test function: test_clean_html_strips_code_blocks
    
    Test cases:
    - Input: '```html\n<p>Content</p>\n```' → should contain <p>Content</p> without backticks or code fence
    - Input: '```\n<p>Content</p>\n```' → should extract content
    - Input: '```json\n{"key": "value"}\n```' → this is JSON, should be handled (unwrap or extract visible text)
    
    Verify: '```' not in result
  </action>
  <verify>
    pytest backend/tests/test_ai_service.py::test_clean_html_strips_code_blocks -x -v 2>&1 | tail -5
  </verify>
  <done>test_clean_html_strips_code_blocks exists and passes</done>
</task>

<task type="auto">
  <name>Task 4: Add test_clean_html_no_markdown</name>
  <files>backend/tests/test_ai_service.py</files>
  <action>
    Add test function: test_clean_html_no_markdown
    
    This test verifies the COMPLETE output has no markdown artifacts:
    - No '```' anywhere
    - No '`' anywhere (backticks)
    - No markdown formatting characters that would display in WordPress
    
    Test with realistic AI output that might contain multiple issues.
  </action>
  <verify>
    pytest backend/tests/test_ai_service.py::test_clean_html_no_markdown -x -v 2>&1 | tail -5
  </verify>
  <done>test_clean_html_no_markdown exists and passes</done>
</task>

<task type="auto">
  <name>Task 5: Add test_clean_html_preserves_allowed_tags</name>
  <files>backend/tests/test_ai_service.py</files>
  <action>
    Add test function: test_clean_html_preserves_allowed_tags
    
    Per D-02: Allowed tags are h1, h2, h3, h4, h5, h6, p, strong, em, ul, ol, li, a, img
    
    Test cases:
    - Input: '<h1>Title</h1><p>Para</p>' → h1 and p preserved
    - Input: '<strong>Bold</strong><em>Italic</em>' → strong and em preserved
    - Input: '<ul><li>Item</li></ul>' → ul and li preserved
    - Input: '<a href="http://example.com">Link</a>' → a with href preserved
    - Input: '<img src="http://example.com/img.jpg">' → img with src preserved
  </action>
  <verify>
    pytest backend/tests/test_ai_service.py::test_clean_html_preserves_allowed_tags -x -v 2>&1 | tail -5
  </verify>
  <done>test_clean_html_preserves_allowed_tags exists and passes</done>
</task>

<task type="auto">
  <name>Task 6: Add test_clean_html_removes_disallowed_tags</name>
  <files>backend/tests/test_ai_service.py</files>
  <action>
    Add test function: test_clean_html_removes_disallowed_tags
    
    Per D-02: Only h1-h6, p, strong, em, ul, ol, li, a, img are allowed. Everything else must be removed.
    
    Test cases:
    - Input: '<script>alert(1)</script><p>Safe</p>' → script removed, p preserved
    - Input: '<div>Wrapper</div><p>Content</p>' → div unwrapped/removed, p preserved
    - Input: '<style>.red{color:red}</style><p>Text</p>' → style removed, p preserved
    - Input: '<iframe src="evil.com"></iframe><p>Text</p>' → iframe removed, p preserved
    - Input: '<span>Inline</span><strong>Bold</strong>' → span removed, strong preserved
    
    Important: Use unwrap() behavior — inner content of removed tags should remain.
  </action>
  <verify>
    pytest backend/tests/test_ai_service.py::test_clean_html_removes_disallowed_tags -x -v 2>&1 | tail -5
  </verify>
  <done>test_clean_html_removes_disallowed_tags exists and passes</done>
</task>

</tasks>

<verification>
- All 5 test functions exist in backend/tests/test_ai_service.py
- All tests pass when run individually
- Full suite passes: pytest backend/tests/test_ai_service.py -x -v
</verification>

<success_criteria>
- backend/tests/test_ai_service.py contains test_clean_html_strips_backticks
- backend/tests/test_ai_service.py contains test_clean_html_strips_code_blocks  
- backend/tests/test_ai_service.py contains test_clean_html_no_markdown
- backend/tests/test_ai_service.py contains test_clean_html_preserves_allowed_tags
- backend/tests/test_ai_service.py contains test_clean_html_removes_disallowed_tags
- All 5 tests pass individually
- Full test suite passes: pytest backend/tests/test_ai_service.py -x -v
</success_criteria>

<output>
After completion, create `.planning/phases/15-html-cleaning-foundation/15-02-PLAN-SUMMARY.md`
</output>

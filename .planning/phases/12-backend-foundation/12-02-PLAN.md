---
phase: 12-backend-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - backend/app/models/post.py
autonomous: true
requirements:
  - LANG-03
  - LANG-08
  - LANG-09
user_setup: []

must_haves:
  truths:
    - PostCreate model has language field with default "vietnamese"
    - BulkPostCreate model has language field with default "vietnamese"
    - PostUpdate model has optional language field with validation
    - PostResponse model has language field
    - Pydantic validates language is "vietnamese" or "english"
    - Invalid language values return 400 error
  artifacts:
    - path: backend/app/models/post.py
      provides: Pydantic models with language field
      contains: "language.*Field.*pattern.*vietnamese.*english"
      min_lines: 100
  key_links:
    - from: backend/app/models/post.py
      to: backend/app/models/ai_provider.py
      via: Pattern validation pattern matching
      pattern: "Field.*pattern.*\\^\\("
    - from: backend/app/models/post.py
      to: backend/app/routers/posts.py
      via: PostCreate, BulkPostCreate, PostUpdate, PostResponse imports
      pattern: "from app.models.post import"
---

<objective>
Add language field to Post Pydantic models with validation. Implement pattern validation matching existing codebase patterns, set default values, and ensure backward compatibility.

Purpose: Establish data model foundation for language support before updating API endpoints.
Output: Updated PostCreate, BulkPostCreate, PostUpdate, PostResponse models with language field.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-backend-foundation/12-RESEARCH.md
@backend/app/models/post.py
@backend/app/models/ai_provider.py
@backend/tests/test_models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add language field to Post models</name>
  <files>backend/app/models/post.py</files>
  <read_first>
    backend/app/models/post.py
    backend/app/models/ai_provider.py
  </read_first>
  <action>
    Update backend/app/models/post.py to add language field:

    1. Import Field from pydantic at top of file (already imported in ai_provider.py pattern):
       ```python
       from pydantic import BaseModel, Field
       ```

    2. Add language field to PostCreate class (after target_section_count field):
       ```python
       language: str = Field(default="vietnamese", pattern="^(vietnamese|english)$")
       ```

    3. Add language field to BulkPostCreate class (after target_section_count field):
       ```python
       language: str = Field(default="vietnamese", pattern="^(vietnamese|english)$")
       ```

    4. Add optional language field to PostUpdate class (after sections field):
       ```python
       language: Optional[str] = Field(None, pattern="^(vietnamese|english)$")
       ```

    5. Add language field to PostResponse class (after origin field):
       ```python
       language: str = "vietnamese"
       ```

    Match the exact pattern from ai_provider.py: `Field(..., pattern="^(openai|gemini|anthropic|openai_compatible)$")`
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_models.py -v</automated>
  </verify>
  <done>
    Post models updated with:
    - PostCreate.language: str with default "vietnamese" and pattern validation
    - BulkPostCreate.language: str with default "vietnamese" and pattern validation
    - PostUpdate.language: Optional[str] with pattern validation
    - PostResponse.language: str with default "vietnamese"
    - All 8 tests in test_models.py pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update format_post() for backward compatibility</name>
  <files>backend/app/routers/posts.py</files>
  <read_first>
    backend/app/routers/posts.py
    backend/app/models/post.py
  </read_first>
  <action>
    Update backend/app/routers/posts.py format_post() function:

    Add language field extraction with default "english" for backward compatibility:
    ```python
    language=doc.get("language", "english"),
    ```

    Insert this line after the target_section_count line (around line 35) and before the title line.
    This ensures existing posts without language field default to "english" while new posts use the Pydantic default "vietnamese".

    The format_post() function should now include language in the PostResponse constructor.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_posts.py::test_backward_compatibility_default -v</automated>
  </verify>
  <done>
    format_post() updated to:
    - Extract language field from MongoDB document
    - Default to "english" for existing posts without language field
    - Include language in PostResponse
    - Backward compatibility test passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model→validation | Pydantic validates language field values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering | PostCreate.language field | mitigate | Pydantic pattern validation restricts to "vietnamese" or "english" only |
| T-12-07 | Tampering | BulkPostCreate.language field | mitigate | Pydantic pattern validation restricts to "vietnamese" or "english" only |
| T-12-08 | Tampering | PostUpdate.language field | mitigate | Pydantic pattern validation restricts to "vietnamese" or "english" only |
| T-12-09 | Denial of Service | Regex pattern validation | accept | Simple regex `^(vietnamese|english)$` has no catastrophic backtracking risk |
</threat_model>

<verification>
After completion, verify:
- PostCreate has language field with default "vietnamese"
- BulkPostCreate has language field with default "vietnamese"
- PostUpdate has optional language field with pattern validation
- PostResponse has language field
- format_post() extracts language with default "english"
- All 8 tests in test_models.py pass
- Backward compatibility test passes
</verification>

<success_criteria>
- PostCreate, BulkPostCreate, PostUpdate, PostResponse have language field
- Pattern validation restricts to "vietnamese" or "english"
- Default for new posts is "vietnamese"
- Default for existing posts (via format_post) is "english"
- All model validation tests pass
- Backward compatibility test passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-backend-foundation/12-02-SUMMARY.md`
</output>

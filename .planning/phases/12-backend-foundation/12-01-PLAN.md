---
phase: 12-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_models.py
  - backend/tests/test_posts.py
  - backend/requirements.txt
autonomous: true
requirements:
  - LANG-03
  - LANG-08
  - LANG-09
user_setup: []

must_haves:
  truths:
    - Test infrastructure exists for running automated tests
    - Pydantic model validation tests can be executed
    - API endpoint tests can be executed
    - MongoDB test database is available for integration tests
  artifacts:
    - path: backend/tests/conftest.py
      provides: Shared test fixtures for MongoDB and test client
      min_lines: 50
    - path: backend/tests/test_models.py
      provides: Pydantic model validation tests
      exports: ["test_post_create_language", "test_bulk_post_create_language", "test_invalid_language_rejected"]
    - path: backend/tests/test_posts.py
      provides: API endpoint integration tests
      exports: ["test_create_post_with_language", "test_backward_compatibility_default", "test_invalid_language_400"]
    - path: backend/requirements.txt
      provides: Test dependencies
      contains: "pytest", "pytest-asyncio", "httpx"
  key_links:
    - from: backend/tests/conftest.py
      to: backend/app/database.py
      via: MongoDB connection fixture
      pattern: "posts_col|projects_col|jobs_col"
    - from: backend/tests/test_models.py
      to: backend/app/models/post.py
      via: Import PostCreate, BulkPostCreate models
      pattern: "from app.models.post import"
    - from: backend/tests/test_posts.py
      to: backend/app/routers/posts.py
      via: Test client hitting /api/posts endpoints
      pattern: "client.post.*posts|client.get.*posts"
---

<objective>
Set up test infrastructure for Phase 12 backend foundation work. Create pytest configuration, shared fixtures for MongoDB and FastAPI test client, and initial test files for model validation and API endpoints.

Purpose: Establish automated testing foundation before implementing language field changes, ensuring we can verify correctness at each step.
Output: Working test suite with fixtures and initial test scaffolds.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-backend-foundation/12-RESEARCH.md
@.planning/phases/12-backend-foundation/12-VALIDATION.md
@backend/app/models/post.py
@backend/app/routers/posts.py
@backend/app/database.py
@backend/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test infrastructure and fixtures</name>
  <files>backend/tests/conftest.py, backend/requirements.txt, backend/pytest.ini</files>
  <read_first>
    backend/app/database.py
    backend/app/main.py
    backend/requirements.txt
  </read_first>
  <action>
    Create test infrastructure:

    1. Add test dependencies to backend/requirements.txt:
       ```
       pytest>=7.4.0
       pytest-asyncio>=0.21.0
       httpx>=0.24.0
       ```

    2. Create backend/pytest.ini with:
       ```ini
       [pytest]
       testpaths = tests
       python_files = test_*.py
       python_classes = Test*
       python_functions = test_*
       asyncio_mode = auto
       ```

    3. Create backend/tests/conftest.py with fixtures:
       - `mongodb_test_db`: Async MongoDB test database fixture using motor
       - `test_client`: FastAPI test client using httpx.AsyncClient
       - `test_project`: Creates a test project in MongoDB for post tests
       - `cleanup_db`: Cleanup fixture to drop test collections after each test

    Use existing database.py patterns for MongoDB connection. Use environment variables MONGODB_URL and MONGODB_DB (default: wordpress_writer_test).
  </action>
  <verify>
    <automated>cd backend && python -m pytest --collect-only tests/</automated>
  </verify>
  <done>
    Test infrastructure created with:
    - pytest.ini configuration file
    - conftest.py with MongoDB and test client fixtures
    - Test dependencies added to requirements.txt
    - pytest can discover tests in tests/ directory
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Pydantic model validation tests</name>
  <files>backend/tests/test_models.py</files>
  <read_first>
    backend/app/models/post.py
    backend/app/models/ai_provider.py
  </read_first>
  <action>
    Create backend/tests/test_models.py with tests for language field validation:

    1. `test_post_create_language_default`: Verify PostCreate defaults language to "vietnamese"
    2. `test_post_create_language_vietnamese`: Verify PostCreate accepts "vietnamese"
    3. `test_post_create_language_english`: Verify PostCreate accepts "english"
    4. `test_post_create_invalid_language_rejected`: Verify PostCreate rejects invalid language (e.g., "spanish")
    5. `test_bulk_post_create_language_default`: Verify BulkPostCreate defaults language to "vietnamese"
    6. `test_bulk_post_create_language_vietnamese`: Verify BulkPostCreate accepts "vietnamese"
    7. `test_bulk_post_create_language_english`: Verify BulkPostCreate accepts "english"
    8. `test_bulk_post_create_invalid_language_rejected`: Verify BulkPostCreate rejects invalid language

    Use pytest.raises(ValidationError) for validation error tests. Import ValidationError from pydantic.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_models.py -v</automated>
  </verify>
  <done>
    test_models.py created with 8 tests covering:
    - Default language value for PostCreate and BulkPostCreate
    - Valid language values ("vietnamese", "english")
    - Invalid language rejection
    - Tests will fail initially (expected - models don't have language field yet)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create API endpoint integration tests</name>
  <files>backend/tests/test_posts.py</files>
  <read_first>
    backend/app/routers/posts.py
    backend/app/models/post.py
  </read_first>
  <action>
    Create backend/tests/test_posts.py with integration tests for language field:

    1. `test_create_post_with_language`: POST /api/posts with language="vietnamese" returns 201 and stores language
    2. `test_create_post_default_language`: POST /api/posts without language defaults to "vietnamese"
    3. `test_create_post_invalid_language_400`: POST /api/posts with invalid language returns 400
    4. `test_get_post_includes_language`: GET /api/posts/{id} returns language field
    5. `test_backward_compatibility_default`: GET /api/posts/{id} for post without language field returns "english"
    6. `test_list_posts_includes_language`: GET /api/posts/by-project/{project_id} returns language for each post
    7. `test_bulk_create_posts_with_language`: POST /api/posts/bulk with language applies to all posts

    Use test_client fixture from conftest.py. Use test_project fixture for project_id. Verify response JSON contains language field.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_posts.py -v</automated>
  </verify>
  <done>
    test_posts.py created with 7 tests covering:
    - POST /api/posts with language field
    - Default language behavior
    - Invalid language validation (400 error)
    - GET /api/posts/{id} returns language
    - Backward compatibility for existing posts
    - List posts includes language
    - Bulk post creation with language
    - Tests will fail initially (expected - endpoints don't handle language yet)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted HTTP requests with language parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | POST /api/posts language field | mitigate | Pydantic pattern validation restricts to "vietnamese" or "english" only (implemented in Plan 02) |
| T-12-02 | Tampering | POST /api/posts/bulk language field | mitigate | Pydantic pattern validation restricts to "vietnamese" or "english" only (implemented in Plan 02) |
| T-12-03 | Tampering | PUT /api/posts/{id} language field | mitigate | Pydantic pattern validation restricts to "vietnamese" or "english" only (implemented in Plan 02) |
| T-12-04 | Denial of Service | Regex pattern validation | accept | Simple regex `^(vietnamese|english)$` has no catastrophic backtracking risk |
| T-12-05 | Information Disclosure | API responses exposing language | accept | Language is not sensitive data, no PII involved |
</threat_model>

<verification>
After completion, verify:
- pytest can discover and run tests in tests/ directory
- conftest.py fixtures are properly loaded
- test_models.py has 8 test functions
- test_posts.py has 7 test functions
- Tests fail initially (expected - models and endpoints not updated yet)
</verification>

<success_criteria>
- Test infrastructure is functional (pytest can run)
- All fixtures in conftest.py are properly defined
- test_models.py exists with 8 test functions
- test_posts.py exists with 7 test functions
- Test dependencies are in requirements.txt
- pytest.ini configuration is present
</success_criteria>

<output>
After completion, create `.planning/phases/12-backend-foundation/12-01-SUMMARY.md`
</output>

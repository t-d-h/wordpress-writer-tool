---
phase: 12-backend-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - backend/app/routers/posts.py
autonomous: true
requirements:
  - LANG-03
  - LANG-08
  - LANG-09
user_setup: []

must_haves:
  truths:
    - POST /api/posts stores language field in MongoDB
    - POST /api/posts includes language in job payload
    - POST /api/posts/bulk passes language to single post creation
    - GET /api/posts/{id} returns language field
    - GET /api/posts/by-project/{project_id} returns language for each post
    - Invalid language returns 400 error
    - Existing posts without language default to "english"
  artifacts:
    - path: backend/app/routers/posts.py
      provides: API endpoints with language support
      contains: '"language": data.language'
      min_lines: 610
  key_links:
    - from: backend/app/routers/posts.py
      to: backend/app/models/post.py
      via: PostCreate, BulkPostCreate models with language field
      pattern: "data.language"
    - from: backend/app/routers/posts.py
      to: backend/app/redis_client.py
      via: publish_job() with language in payload
      pattern: "publish_job.*language"
    - from: backend/app/routers/posts.py
      to: backend/app/database.py
      via: posts_col.insert_one() with language field
      pattern: "posts_col.insert_one.*language"
---

<objective>
Update API endpoints to handle language field. Store language in MongoDB, include in job payloads, and ensure all endpoints properly handle language parameter.

Purpose: Complete backend foundation for language support by updating all API endpoints.
Output: Updated POST /api/posts, POST /api/posts/bulk, GET endpoints with language support.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-backend-foundation/12-RESEARCH.md
@backend/app/routers/posts.py
@backend/app/models/post.py
@backend/tests/test_posts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update create_post() to store and propagate language</name>
  <files>backend/app/routers/posts.py</files>
  <read_first>
    backend/app/routers/posts.py
    backend/app/models/post.py
  </read_first>
  <action>
    Update backend/app/routers/posts.py create_post() function:

    1. Add language field to post_doc dictionary (after target_section_count line, around line 100):
       ```python
       "language": data.language,
       ```

    2. Add language field to job payload in publish_job() call (after target_word_count line, around line 166):
       ```python
       "language": data.language,
       ```

    This ensures:
    - Language is stored in MongoDB posts collection
    - Language is included in research job payload for downstream processing
    - Language flows through the entire AI pipeline

    The create_post() function should now store and propagate language to both MongoDB and job payloads.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_posts.py::test_create_post_with_language tests/test_posts.py::test_create_post_default_language tests/test_posts.py::test_create_post_invalid_language_400 -v</automated>
  </verify>
  <done>
    create_post() updated to:
    - Store language field in MongoDB post document
    - Include language in research job payload
    - POST /api/posts with language="vietnamese" test passes
    - POST /api/posts without language defaults to "vietnamese" test passes
    - POST /api/posts with invalid language returns 400 test passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Update create_bulk_posts() to pass language</name>
  <files>backend/app/routers/posts.py</files>
  <read_first>
    backend/app/routers/posts.py
    backend/app/models/post.py
  </read_first>
  <action>
    Update backend/app/routers/posts.py create_bulk_posts() function:

    Add language field to PostCreate instantiation in the loop (after target_section_count line, around line 193):
    ```python
    language=data.language,
    ```

    This ensures bulk post creation passes the language field from BulkPostCreate to each individual PostCreate instance, which then flows through create_post() to MongoDB and job payloads.

    The create_bulk_posts() function should now pass language to each single post creation.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_posts.py::test_bulk_create_posts_with_language -v</automated>
  </verify>
  <done>
    create_bulk_posts() updated to:
    - Pass language field from BulkPostCreate to each PostCreate
    - POST /api/posts/bulk with language applies to all posts test passes
    - All posts created via bulk endpoint have language field
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify all GET endpoints return language</name>
  <files>backend/tests/test_posts.py</files>
  <read_first>
    backend/app/routers/posts.py
    backend/tests/test_posts.py
  </read_first>
  <action>
    Verify GET endpoints return language field:

    1. Run existing tests for GET endpoints:
       - test_get_post_includes_language
       - test_list_posts_includes_language

    2. These tests should already pass because format_post() was updated in Plan 02 to extract language from MongoDB documents.

    3. If tests fail, verify format_post() includes language in PostResponse constructor.

    No code changes needed - just verification that GET endpoints work correctly with the format_post() update from Plan 02.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_posts.py::test_get_post_includes_language tests/test_posts.py::test_list_posts_includes_language -v</automated>
  </verify>
  <done>
    GET endpoints verified:
    - GET /api/posts/{id} returns language field
    - GET /api/posts/by-project/{project_id} returns language for each post
    - All GET endpoint tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted HTTP requests with language parameter |
| API→MongoDB | Language field stored in database |
| API→Redis | Language field in job payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Tampering | POST /api/posts language field | mitigate | Pydantic pattern validation restricts to "vietnamese" or "english" only (Plan 02) |
| T-12-11 | Tampering | POST /api/posts/bulk language field | mitigate | Pydantic pattern validation restricts to "vietnamese" or "english" only (Plan 02) |
| T-12-12 | Tampering | MongoDB language field | mitigate | Motor driver handles escaping, Pydantic validates input before storage |
| T-12-13 | Tampering | Redis job payload language field | mitigate | Pydantic validates input before job creation, Redis pub/sub is internal |
| T-12-14 | Information Disclosure | API responses exposing language | accept | Language is not sensitive data, no PII involved |
</threat_model>

<verification>
After completion, verify:
- POST /api/posts stores language in MongoDB
- POST /api/posts includes language in job payload
- POST /api/posts/bulk passes language to single post creation
- GET /api/posts/{id} returns language field
- GET /api/posts/by-project/{project_id} returns language for each post
- Invalid language returns 400 error
- All 7 tests in test_posts.py pass
</verification>

<success_criteria>
- POST /api/posts stores language field in MongoDB
- POST /api/posts includes language in job payload
- POST /api/posts/bulk passes language to each post
- GET /api/posts/{id} returns language field
- GET /api/posts/by-project/{project_id} returns language for each post
- Invalid language returns 400 error
- All 7 tests in test_posts.py pass
- All 8 tests in test_models.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-backend-foundation/12-03-SUMMARY.md`
</output>

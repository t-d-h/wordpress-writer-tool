---
phase: 13-ai-service-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/ai_service.py
autonomous: true
requirements:
  - LANG-04
must_haves:
  truths:
    - "research_topic() accepts language parameter with default 'vietnamese'"
    - "generate_outline() accepts language parameter with default 'vietnamese'"
    - "generate_section_content() accepts language parameter with default 'vietnamese'"
    - "generate_introduction() accepts language parameter with default 'vietnamese'"
    - "generate_full_content() accepts language parameter with default 'vietnamese'"
    - "Language parameter is passed to _call_ai() in all functions"
  artifacts:
    - path: "backend/app/services/ai_service.py"
      provides: "AI service functions with language parameter"
      min_lines: 401
      contains: "language: str = \"vietnamese\""
  key_links:
    - from: "backend/app/services/ai_service.py"
      to: "worker/app/workers/tasks.py"
      via: "Function signature with language parameter"
      pattern: "def research_topic.*language.*str"
---

<objective>
Add language parameter to all AI service functions to support Vietnamese and English content generation.

Purpose: Enable AI service functions to receive language parameter so content can be generated in the correct language (Vietnamese or English).

Output: Updated AI service functions with language parameter support.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ai-service-integration/13-RESEARCH.md
@backend/app/services/ai_service.py
@worker/app/workers/tasks.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add language parameter to research_topic()</name>
  <files>backend/app/services/ai_service.py</files>
  <action>Add `language: str = "vietnamese"` parameter to `research_topic()` function signature (line 195). Update function to accept and store the language parameter. Do not modify system prompts yet — that's Plan 02. Just add the parameter to the function signature and pass it through to any internal calls if needed.</action>
  <verify>grep -n "def research_topic" backend/app/services/ai_service.py shows language parameter in signature</verify>
  <done>research_topic() function signature includes language: str = "vietnamese" parameter</done>
</task>

<task type="auto">
  <name>Task 2: Add language parameter to generate_outline()</name>
  <files>backend/app/services/ai_service.py</files>
  <action>Add `language: str = "vietnamese"` parameter to `generate_outline()` function signature (line 233). Update function to accept and store the language parameter. Do not modify system prompts yet — that's Plan 02. Just add the parameter to the function signature.</action>
  <verify>grep -n "def generate_outline" backend/app/services/ai_service.py shows language parameter in signature</verify>
  <done>generate_outline() function signature includes language: str = "vietnamese" parameter</done>
</task>

<task type="auto">
  <name>Task 3: Add language parameter to generate_section_content()</name>
  <files>backend/app/services/ai_service.py</files>
  <action>Add `language: str = "vietnamese"` parameter to `generate_section_content()` function signature (line 286). Update function to accept and store the language parameter. Do not modify system prompts yet — that's Plan 02. Just add the parameter to the function signature.</action>
  <verify>grep -n "def generate_section_content" backend/app/services/ai_service.py shows language parameter in signature</verify>
  <done>generate_section_content() function signature includes language: str = "vietnamese" parameter</done>
</task>

<task type="auto">
  <name>Task 4: Add language parameter to generate_introduction()</name>
  <files>backend/app/services/ai_service.py</files>
  <action>Add `language: str = "vietnamese"` parameter to `generate_introduction()` function signature (line 321). Update function to accept and store the language parameter. Do not modify system prompts yet — that's Plan 02. Just add the parameter to the function signature.</action>
  <verify>grep -n "def generate_introduction" backend/app/services/ai_service.py shows language parameter in signature</verify>
  <done>generate_introduction() function signature includes language: str = "vietnamese" parameter</done>
</task>

<task type="auto">
  <name>Task 5: Add language parameter to generate_full_content()</name>
  <files>backend/app/services/ai_service.py</files>
  <action>Add `language: str = "vietnamese"` parameter to `generate_full_content()` function signature (line 352). Update function to accept the language parameter and pass it to `generate_introduction()` and `generate_section_content()` calls. Do not modify system prompts yet — that's Plan 02. Just add the parameter and pass it through.</action>
  <verify>grep -n "def generate_full_content" backend/app/services/ai_service.py shows language parameter in signature</verify>
  <done>generate_full_content() function signature includes language: str = "vietnamese" parameter and passes it to sub-functions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → AI Service | Language parameter flows from API through job payload to AI service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | Language parameter in AI service functions | accept | Parameter already validated at API layer (Phase 12), no additional validation needed in AI service |
| T-13-02 | Tampering | System prompt injection via language parameter | accept | Language parameter restricted to "vietnamese" or "english" by Phase 12 validation, no injection risk |
</threat_model>

<verification>
- All AI service functions have language parameter with default "vietnamese"
- Function signatures match the pattern: `language: str = "vietnamese"`
- generate_full_content() passes language to generate_introduction() and generate_section_content()
</verification>

<success_criteria>
- research_topic() accepts language parameter
- generate_outline() accepts language parameter
- generate_section_content() accepts language parameter
- generate_introduction() accepts language parameter
- generate_full_content() accepts language parameter and passes to sub-functions
- All functions have default value "vietnamese"
</success_criteria>

<output>
After completion, create `.planning/phases/13-ai-service-integration/13-01-SUMMARY.md`
</output>

---
phase: 13-ai-service-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - backend/app/services/ai_service.py
autonomous: true
requirements:
  - LANG-05
must_haves:
  truths:
    - "research_topic() system prompt includes language-specific instruction"
    - "generate_outline() system prompt includes language-specific instruction"
    - "generate_section_content() system prompt includes language-specific instruction"
    - "generate_introduction() system prompt includes language-specific instruction"
    - "Vietnamese prompts include cultural context and formality instruction"
    - "English prompts include language instruction"
  artifacts:
    - path: "backend/app/services/ai_service.py"
      provides: "Language-specific system prompts"
      min_lines: 401
      contains: "if language == \"vietnamese\":"
  key_links:
    - from: "backend/app/services/ai_service.py"
      to: "AI providers (OpenAI, Gemini, Anthropic)"
      via: "System prompt with language instruction"
      pattern: "system_prompt.*vietnamese.*english"
---

<objective>
Modify system prompts in AI service functions to include language-specific instructions for Vietnamese and English content generation.

Purpose: Ensure AI generates content in the correct language with appropriate cultural context and formality level.

Output: Updated system prompts with language-specific instructions.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-ai-service-integration/13-RESEARCH.md
@.planning/phases/13-ai-service-integration/13-01-PLAN.md
@backend/app/services/ai_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add language-specific system prompt to research_topic()</name>
  <files>backend/app/services/ai_service.py</files>
  <action>Modify `research_topic()` to construct language-specific system prompts. Before the existing system_prompt assignment (line 202), add conditional logic:

```python
# Language-specific system prompt
if language == "vietnamese":
    system_prompt = (
        "You are an expert SEO content researcher for Vietnamese content. "
        "Write all content in Vietnamese. Use formal, professional Vietnamese "
        "with appropriate cultural context. Respond only in valid JSON."
    )
else:  # english
    system_prompt = (
        "You are an expert SEO content researcher. "
        "Write all content in English. Respond only in valid JSON."
    )
```

Replace the existing hardcoded system_prompt with this conditional logic.</action>
  <verify>grep -A 5 "if language == \"vietnamese\":" backend/app/services/ai_service.py shows conditional system prompt in research_topic()</verify>
  <done>research_topic() has language-specific system prompt with Vietnamese cultural context</done>
</task>

<task type="auto">
  <name>Task 2: Add language-specific system prompt to generate_outline()</name>
  <files>backend/app/services/ai_service.py</files>
  <action>Modify `generate_outline()` to construct language-specific system prompts. Before the existing system_prompt assignment (line 242), add conditional logic:

```python
# Language-specific system prompt
if language == "vietnamese":
    system_prompt = (
        "You are an expert SEO content strategist for Vietnamese content. "
        "Write all content in Vietnamese. Use formal, professional Vietnamese "
        "with appropriate cultural context. Respond only in valid JSON."
    )
else:  # english
    system_prompt = (
        "You are an expert SEO content strategist. "
        "Write all content in English. Respond only in valid JSON."
    )
```

Replace the existing hardcoded system_prompt with this conditional logic.</action>
  <verify>grep -A 5 "if language == \"vietnamese\":" backend/app/services/ai_service.py shows conditional system prompt in generate_outline()</verify>
  <done>generate_outline() has language-specific system prompt with Vietnamese cultural context</done>
</task>

<task type="auto">
  <name>Task 3: Add language-specific system prompt to generate_section_content()</name>
  <files>backend/app/services/ai_service.py</files>
  <action>Modify `generate_section_content()` to construct language-specific system prompts. Replace the existing hardcoded system_prompt (line 297) with conditional logic:

```python
# Language-specific system prompt
if language == "vietnamese":
    system_prompt = (
        "You are an expert blog content writer for Vietnamese content. "
        "Write engaging, detailed, SEO-optimized content in Vietnamese. "
        "Use formal, professional Vietnamese with appropriate cultural context."
    )
else:  # english
    system_prompt = (
        "You are an expert blog content writer. "
        "Write engaging, detailed, SEO-optimized content in English."
    )
```

Replace the existing hardcoded system_prompt with this conditional logic.</action>
  <verify>grep -A 5 "if language == \"vietnamese\":" backend/app/services/ai_service.py shows conditional system prompt in generate_section_content()</verify>
  <done>generate_section_content() has language-specific system prompt with Vietnamese cultural context</done>
</task>

<task type="auto">
  <name>Task 4: Add language-specific system prompt to generate_introduction()</name>
  <files>backend/app/services/ai_service.py</files>
  <action>Modify `generate_introduction()` to construct language-specific system prompts. Replace the existing hardcoded system_prompt (line 330) with conditional logic:

```python
# Language-specific system prompt
if language == "vietnamese":
    system_prompt = (
        "You are an expert blog content writer for Vietnamese content. "
        "Write engaging introductions in Vietnamese. "
        "Use formal, professional Vietnamese with appropriate cultural context."
    )
else:  # english
    system_prompt = (
        "You are an expert blog content writer. "
        "Write engaging introductions in English."
    )
```

Replace the existing hardcoded system_prompt with this conditional logic.</action>
  <verify>grep -A 5 "if language == \"vietnamese\":" backend/app/services/ai_service.py shows conditional system prompt in generate_introduction()</verify>
  <done>generate_introduction() has language-specific system prompt with Vietnamese cultural context</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI Service → AI Providers | System prompts with language instruction sent to AI providers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Tampering | System prompt injection via language parameter | accept | Language parameter restricted to "vietnamese" or "english" by Phase 12 validation, no injection risk |
| T-13-04 | Information Disclosure | Cultural context in prompts | accept | No sensitive information in prompts, only general cultural context instructions |
</threat_model>

<verification>
- All AI service functions have conditional system prompts based on language
- Vietnamese prompts include cultural context and formality instruction
- English prompts include language instruction
- System prompts are constructed before _call_ai() invocation
</verification>

<success_criteria>
- research_topic() has language-specific system prompt
- generate_outline() has language-specific system prompt
- generate_section_content() has language-specific system prompt
- generate_introduction() has language-specific system prompt
- Vietnamese prompts include cultural context and formality instruction
- English prompts include language instruction
</success_criteria>

<output>
After completion, create `.planning/phases/13-ai-service-integration/13-02-SUMMARY.md`
</output>

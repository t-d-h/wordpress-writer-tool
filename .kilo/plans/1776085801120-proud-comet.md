# Plan: Remove Section Images Stage from Pipeline

## Overview
Remove the "section images" stage from the content generation pipeline. The pipeline will now be: research → outline → content → thumbnail → publish (instead of research → outline → content → thumbnail → section_images → publish).

## Changes Required

### 1. Frontend - PostView.jsx
**File**: `frontend/src/components/Posts/PostView.jsx`

**Changes**:
- Remove "section_images" step from `pipelineSteps` array (line 142)
- Remove "Generate Section Images" button (lines 183-185)
- Remove section_images expanded stage content (lines 409-419)
- Remove section_images token usage display (lines 206-209)
- Remove `section_images` action from `handleAction` function (line 77)
- Remove `generateSectionImages` import (line 4)

### 2. Frontend - client.js
**File**: `frontend/src/api/client.js`

**Changes**:
- Remove `generateSectionImages` function (line 62)
- Remove `uploadSectionImages` function (lines 64-70)

### 3. Frontend - ProjectDetail.jsx
**File**: `frontend/src/components/Projects/ProjectDetail.jsx`

**Changes**:
- Remove `section_images_source`, `section_images_provider_id`, `section_images_model_name`, `section_image_files` from form state initialization (lines 26-27, 43-44, 85-87, 103-105, 149-150, 155, 166-167, 172, 293-294, 299, 366-367, 372)
- Remove section_images form fields (lines 688-726 for single form, lines 855-893 for bulk form)
- Remove section_images validation (lines 238-241, 323-326)
- Remove section_images from formData append (lines 265-267, 350-352)
- Remove section_images from resetForm (lines 366-367, 372)
- Remove `generateSectionImages` from action handlers (line 389)
- Remove section_images conditional rendering (lines 415, 428, 446, 461, 470)
- Remove `fetchModelsForProvider` call for section_images (lines 123, 133)

### 4. Backend - tasks.py
**File**: `worker/app/workers/tasks.py`

**Changes**:
- Remove `run_section_images` function (lines 348-409)
- Update `run_thumbnail` to queue "publish" instead of "section_images" (line 339)
- Update log message to reflect new pipeline (line 340)

### 5. Backend - redis_worker.py
**File**: `worker/app/workers/redis_worker.py`

**Changes**:
- Remove `run_section_images` import (line 18)
- Remove "section_images" from TASK_MAP (line 29)

### 6. Backend - posts.py
**File**: `backend/app/routers/posts.py`

**Changes**:
- Remove `upload_section_images` endpoint (lines 473-510)
- Remove `generate_section_images` endpoint (lines 513-550)
- Remove section_images fields from `format_post` helper (lines 33-35)
- Remove section_images fields from post creation (lines 93-95, 191-193)
- Remove section_images from token_usage initialization (line 115)

### 7. Backend - models/post.py
**File**: `backend/app/models/post.py`

**Changes**:
- Remove `section_images_source`, `section_images_provider_id`, `section_images_model_name` from `PostCreate` (lines 22-24)
- Remove `section_images_source`, `section_images_provider_id`, `section_images_model_name` from `BulkPostCreate` (lines 39-41)
- Remove `section_images_source`, `section_images_provider_id`, `section_images_model_name` from `PostResponse` (lines 83-85)
- Remove `sections_done` from `PostResponse` (line 99)
- Remove `section_images` from `TokenUsage` (line 68)
- Update `JobInfo` comment to remove section_images reference (line 56)

### 8. Backend - image_service.py
**File**: `backend/app/services/image_service.py`

**Changes**:
- Remove `generate_section_image` function (if exists)

## Pipeline Flow After Changes

**Before**: research → outline → content → thumbnail → section_images → publish

**After**: research → outline → content → thumbnail → publish

## Testing Considerations

1. Verify pipeline progression works correctly: thumbnail → publish
2. Verify auto_publish flag still works correctly
3. Verify no orphaned section_images references in UI
4. Verify post creation still works without section_images fields
5. Verify token usage no longer shows section_images

## Notes

- The `sections` array in the post model will remain (used for content structure)
- Only the image generation for sections is being removed
- The `sections_done` flag will be removed as it's no longer needed

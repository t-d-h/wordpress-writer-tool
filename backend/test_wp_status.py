import asyncio
import httpx

async def main():
    # just checking our own API? no, let's just make a mock WP call if we had credentials, but we don't.
    pass

if __name__ == "__main__":
    asyncio.run(main())
